## FR5.12 embedding 语义检索优化 — 测试报告

**日期**: 2026-08-13
**对应方案**: `plans/fr5.12-embedding-optimization/plan.md`

### 测试覆盖
| 测试项 | 状态 | 备注 |
|--------|------|------|
| 批量语义召回 + 阈值统一（S1） | ✅ | search_batch 批量、放开 [:10]、阈值 0.7 一致 |
| 增量索引 + 编码缓存（S2） | ✅ | 新增/修改/删除软删除、20% 压缩、LRU 缓存命中 |
| BM25 混合检索（S3） | ✅ | rank_bm25 融合、加权求和、缺失降级 |
| 跨会话加载 _row_map 重建（Critical 修复） | ✅ | 加载已有索引后融合检索向量分不丢失 |
| 全量回归 | ✅ | 550 passed（基线 540 + 新增 10），零回归 |

### 审查结论
- **方案一致性**: ✅ 三项增强均实现，符合 ADR-013（rank_bm25 / 加权求和 / ID 映射软删除 / LRU 512）
- **代码质量**: ✅ Critical（加载已有索引后 _row_map 未重建）已修复并补回归测试
- **安全性**: ✅ 无注入/越权/敏感信息问题；rank_bm25 为纯 Python 库无安全风险

### 发现的问题
- [已修复] Critical: `_try_load_index` 加载已有索引后未重建 `_row_map`，导致融合检索向量分丢失 → 已补重建逻辑 + 回归测试
- [已修复] Minor: 测试缺跨会话加载场景覆盖 → 已补 `test_load_index_rebuilds_row_map`

### 签名
QA 通过
