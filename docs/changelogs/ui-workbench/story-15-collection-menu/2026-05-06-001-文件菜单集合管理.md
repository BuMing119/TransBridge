# 001: 文件菜单 — 集合管理

**日期**: 2026-05-06
**类型**: 改
**关联**: Epic: UI 工作台 > Story 15: 文件菜单 — 集合管理

## 修改文件

### `src/transbridge/ui/main_window.py` (改)
- **修改内容**: `_init_menu()` 中「文件」菜单重构，新增「集合」子菜单（新建集合 / 导入 JSON… / 移除当前集合 / 分隔线 / 切换集合子菜单）。新增方法：`_rebuild_collection_menu()` 根据 `ctx.slots` 动态重建切换集合列表；`_update_collection_menu_state()` 根据槽位是否为空控制移除和切换菜单项的启用/禁用；`_on_new_slot()` 清除当前激活状态；`_on_import_json()` 弹出文件对话框→后台 ApiWorker 加载→注册 slot（逻辑从 step1.py 提取）；`_on_remove_slot()` 弹出确认框→调用 `ctx.remove_slot()`。连接 `ctx.collection_list_changed` 信号到 `_on_collection_list_changed()` 触发菜单重建和状态更新
- **原因**: FR7.7.1 需求——将 Step1 面板中的集合管理控件（新建/导入JSON/移除/切换下拉框）迁移到文件菜单，为后续移除 Step1 面板做准备
