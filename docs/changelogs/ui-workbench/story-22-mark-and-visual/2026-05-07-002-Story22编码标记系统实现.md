# 002: Story-22 编码 — 标记列与可视化系统

**日期**: 2026-05-07
**类型**: 改/删
**关联**: Epic: UI 工作台 > Story 22: 标记列与可视化系统

## 修改文件

### `src/transbridge/ui/workbench/step2.py` (改/删)
- **修改内容**:
  - **标记列替换复选框**: `_COL_CHECK`→`_COL_MARK`，新增 `_MARK_TYPES`/`_MARK_CYCLE`/`_MARK_COLORS`/`_MARK_LABELS` 常量；`_selected_entry_ids: set[str]`→`_entry_marks: dict[str, str]`；`_populate_table` 中 Col 0 从 `ItemIsUserCheckable` 复选框改为显示标记字符的普通文本项（★/?/✓）
  - **行背景色**: 新增 `_ROW_BG_YELLOW`(#FFF8E1)/`_ROW_BG_GREEN`(#E8F5E9)；`_populate_table` 中按 stage 设置整行背景色；`_on_item_changed` 译文编辑后同步更新行背景色
  - **标记筛选标签**: 新增 `_mark_tags_widget`/`_mark_tags_container`；`_mark_filters: set[str]`；`_build_mark_tags()` 统计并创建 ★/?/✓ 标签按钮；`_on_mark_tag_clicked()` 多选切换
  - **聚焦开关**: 新增 `_focus_btn` QPushButton（👁 只看已标记，toggle 模式）；`_focus_marked: bool`；`_on_focus_toggle()` 切换聚焦
  - **标记计数**: `_update_count_label` 改为「★ N / ? N / ✓ N | 显示 M 条（共 K 条）」
  - **`_apply_all_filters` 扩展**: 叠加 `_mark_filters` 和 `_focus_marked` 筛选
  - **`get_selected_entries` 适配**: 返回 ★("star")标记条目（向后兼容 AI 翻译窗口）
  - **`_on_cell_clicked` 重写**: 仅处理标记列点击，循环切换：无→★→?→✓→无
  - **`_on_item_changed` 简化**: 移除复选框分支，仅保留译文编辑逻辑
  - **`refresh()` 更新**: 清理 `_entry_marks` 失效条目，重建 `_build_mark_tags`，重置聚焦状态
  - **移除**: `_selected_entry_ids`、`_last_clicked_row`、所有复选框/行选 Ctrl/Shift 逻辑
- **原因**: FR7.10 需求——将复选框选中机制升级为三态标记系统，配合行背景色和聚焦功能，使 Step2 成为真正的翻译审校工作台
