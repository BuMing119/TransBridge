# 003: 聚焦按钮修复 + 去除 emoji

**日期**: 2026-05-07
**类型**: 改
**关联**: Epic: UI 工作台 > Story 22: 标记列与可视化系统

## 修改文件

### `src/transbridge/ui/workbench/step2.py` (改)
- **修改内容**:
  - `_focus_btn` 文本从 `"👁 只看已标记"` 改为 `"[已标记]"`（去除 emoji）
  - `_focus_btn` 移除 `setCheckable(True)`（Qt Checkable + clicked 信号在部分环境下行为异常）
  - `_on_focus_toggle` 签名从 `(self, checked: bool)` 改为 `(self)`，手动切换 `_focus_marked`
  - 3 处 `_focus_btn.setChecked(False)` 改为 `_focus_btn.setText("[已标记]")`（Checkable 已移除）
- **原因**: 聚焦按钮无法点击（`setCheckable(True)` 交互问题）；去除 emoji 保持代码纯文本
