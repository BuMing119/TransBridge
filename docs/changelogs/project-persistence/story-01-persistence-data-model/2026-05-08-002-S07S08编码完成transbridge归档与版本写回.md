# 002: S07-S08 编码完成 — .transbridge 归档与版本写回

**日期**: 2026-05-08
**类型**: 增
**关联**: Epic: 项目持久化与翻译版本管理 > Story 07-08

## 修改文件

### `src/transbridge/ui/main_window.py` (改)
- **修改内容**: S07 .transbridge 导入导出——文件菜单新增「导出 .transbridge…」「导入 .transbridge…」菜单项，`_on_export_transbridge()` 使用 zipfile 将项目目录所有 JSON 文件打包为 ZIP，`_on_import_transbridge()` 解压到 data/projects/ 并检测项目名冲突（覆盖/取消），导入后自动加载项目+版本
- **原因**: FR8.8 要求 .transbridge 单体文件作为项目分享/备份的便携格式

### `src/transbridge/ui/workbench/cards/write_card.py` (改)
- **修改内容**: S08 版本写回——WriteCard 新增 `_output_dir_override` 属性，`_do_write()` 在多版本项目时弹出「写回模式」确认对话框（仅当前版本/所有版本），`_write_all_variants()` 遍历所有版本→切换到目标版本的译文→写回到 `{output_dir}/{variant_name}/` 子目录→恢复原始版本，`_write_single()` 提取原有写回逻辑
- **原因**: FR8.12 要求分版本分目录输出，不修改源文件名
