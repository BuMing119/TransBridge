# Story 02：公共 Operation 与 Capability 合同

- 日期：2026-08-18
- Epic/Story：`platform-contract-foundation-v2/S02`
- 追溯：FR17.1～17.3、FR20.1、ADR-016/019、R-003/R-005
- 状态：实现完成，增量验证通过；待综合 QA

## 实现增量

- 新增 transport-neutral `RequestContext`、`OperationResult`、互斥 `OperationOutcome`、`Diagnostic`、`OperationCounts`、`DomainError` 与 `Deferred[JobRef]`。
- 建立 input/prerequisite/permission/conflict/external/cancel/internal 异常分类及默认脱敏映射，异常不能转换为 completed。
- 新增 schema v1 序列化、向后兼容读取与 JSON Schema；partial/failed/cancelled 的计数、诊断和值/产物约束由类型构造时校验。
- 新增线程安全 `CapabilityRegistry` 和 available/degraded/unavailable 报告；S01 的依赖探针已复用该公共合同。
- 提供历史 ToolResult adapter，保留 partial、failed items、diagnostics 与 cancel 语义。

## 验证证据

- `uv run --locked pytest tests/contracts/test_operation_contracts.py ...`：31 passed。
- 与 packaging capability 集成复验：38 passed。
- 全量回归：644 passed；Ruff/format/compileall 通过。
- [EvidenceManifest](../../../test-reports/requirement-code-review-2026-08-18/qa-evidence/platform-s02/qa-20260818T062045.181590Z-45ec5809386f/manifest.json)：验证结果 `passed`，schema/verdict/hash 复验有效。
- 未执行 Git commit/push。

## 剩余门禁

真实 adapter、RuntimeContext 和入口使用该合同的证据由 S03～S05 建立；当前完成不等于 Composition Root 或多入口等价性已验收。
