# Story 01：包、入口、版本与依赖基线

- 日期：2026-08-18
- Epic/Story：`platform-contract-foundation-v2/S01`
- 追溯：FR17.2/17.5、NFR3.1/NFR6.1、ADR-016、R-001/R-002
- 状态：实现完成，增量验证通过；待综合 QA

## 实现增量

- `pyproject.toml` 成为版本与入口权威源；包版本通过 distribution metadata 读取，源码态仅回读同一项目元数据。
- `transbridge` 与 `transbridge-mcp` 使用 headless-safe 参数入口，GUI/MCP 实现仅在实际执行时惰性导入。
- 生产代码与测试 patch target 从 `src.transbridge` 收口到安装态 `transbridge`；保留包根 `main()` compatibility facade。
- `rank-bm25`、`py7zr`、`rarfile` 已进入声明和 `uv.lock`，PyInstaller 收集依赖与 distribution metadata；缺失依赖通过公共 Capability 合同报告 unavailable。
- Inno Setup 版本由 `build.bat` 从 `pyproject.toml` 注入；构建环境严格使用 `uv sync --frozen`，不再临时 `uv add`。

## 验证证据

- `uv run --locked pytest tests/packaging/test_package_baseline.py ...`：7 passed。
- `uv run --locked pytest ...` 全量回归：644 passed，15 个既有 deprecation warnings。
- `uv run --isolated --locked --no-editable --no-group dev ...`：Python 3.12.12，安装态版本 0.1.1.1，包导入不加载 PyQt，CLI/MCP `--help` 均返回 0。
- [EvidenceManifest](../../../test-reports/requirement-code-review-2026-08-18/qa-evidence/platform-s01/qa-20260818T062041.485189Z-77706e96f3d9/manifest.json)：验证结果 `passed`，schema/verdict/hash 复验有效。
- Ruff、compileall、`git diff --check` 通过；未执行 Git commit/push。

## 证据校正与剩余门禁

现有 uv 环境可用；先前“`.venv` 损坏”是沙箱不能访问 uv 受管 Python 目录造成的误判。Installer/onedir 实物、许可证和卸载行为仍由 release-hardening-v2/S05 验证，本记录不提前宣称发行验收完成。
