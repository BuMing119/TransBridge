# 003: QA 第四轮收尾 — 修复最后 3 项遗留问题

**日期**: 2026-05-13
**类型**: 改
**关联**: Epic: Smart Assistant QA 修复 > QA Round 4 收尾修复

## 修改文件

### `src/transbridge/smart_assistant/agents/orchestrator.py` (改)
- **修改内容**: m33: `map_to_steps()` 中删除 `or getattr(st, 'tool_name', '')` 死代码。`Subtask` dataclass 不包含 `tool_name` 属性，此 getattr 永远返回 `''`
- **原因**: 死代码阅读混淆

### `src/transbridge/smart_assistant/memory/memory_retriever.py` (改)
- **修改内容**: m29: 模块和类 docstring 更新，明确说明 embedding_client 为外部依赖（由调用方传入，如 ai_translator.EmbeddingClient），不传入时仅使用关键词精确匹配。`__init__` 添加注释标注外部注入
- **原因**: 消除"依赖不存在的 memory/embedding.py 模块"的困惑

### `tests/test_execution_engine.py` (改)
- **修改内容**: CR14: 移除 `test_execute_linear_graph`、`test_execute_single_node`、`test_execute_results_order`、`test_cancel_stops_execution` 4 个测试的 `@unittest.skip` 装饰器。测试文件已具备完整 mock 基础设施（FakeToolRegistry、FakeToolSpec、FakeCtx、QApplication）
- **原因**: 测试基础设施已就绪，skip 为预判性跳过而非实际阻塞问题
