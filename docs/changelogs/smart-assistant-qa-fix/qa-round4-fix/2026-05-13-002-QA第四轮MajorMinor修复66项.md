# 002: QA 第四轮 Major/Minor 修复 — 66 项全覆盖

**日期**: 2026-05-13
**类型**: 改
**关联**: Epic: Smart Assistant QA 修复 > QA Round 4 Major/Minor 修复批次

## 修改概览

6 个并行 Agent 修复 28 Major + 38 Minor = 66 项问题，覆盖 ~30 文件。

---

## Agent-1: execution_engine.py (5项)

### `src/transbridge/smart_assistant/execution_engine.py` (改)
- **修改内容**: (1) M22: `_run_single` 123行拆分为 `_await_decision()` (10行) + `_run_guard_chain()` (57行)，主方法缩减为 84 行 (-32%)；(2) M23: `execute_graph` 125行拆分为 `_dispatch_node()` (51行) + `_bfs_one_level()` (45行)，主方法缩减为 38 行 (-70%)；(3) m7: `_eval_ast_node` 添加 `_MAX_EVAL_DEPTH = 50` 递归深度限制，所有 11 处递归传入 depth+1；(4) m11: `_safe_serialize` 添加 `isinstance(value, QObject)` 检测；(5) m32: 确认超时 300s 提取为 `_CONFIRM_TIMEOUT` 类常量
- **原因**: 降低圈复杂度提升可测试性，防止恶意嵌套栈溢出，Qt 对象 str() 泄露内存地址

---

## Agent-2: chat_widget.py (11项)

### `src/transbridge/ui/tools/smart_assistant/chat_widget.py` (改)
- **修改内容**: (1) M5: `_on_plan_all_finished` 调用 `_run_llm_round()` 前添加 `_check_react_depth()` 检查；(2) M14: `_flush_streaming` 流式期间改用 QLabel 纯文本替代 MarkdownRenderer 完整 widget 树重建，流式结束后一次性渲染；(3) M15: `_clear_conversation` widget 移除从 `takeAt(0)` 改为 `takeAt(count-2)` 从末尾移除，O(n²)→O(n)；(4) M18: memory 检索通过 `QTimer.singleShot(0)` 延迟到事件循环，新增 `_do_send_retrieve_and_run()` 方法；(5) m1: `_on_llm_error` 添加 `_streaming_bubble` 清理；(6) m2: 重试按钮改为实例变量复用，防止叠加；(7) m3: scroll 处理添加 100ms 节流 timer；(8) m12: `max_tokens` 从 LLMConfig 读取替代硬编码 2048；(9) m38: 文件上传异常消息中过滤绝对路径
- **原因**: 流式渲染性能优化、主线程阻塞消除、O(n²)卡顿修复、配置灵活性提升、错误信息防泄露

---

## Agent-3: memory + conversation + context (9项)

### `src/transbridge/smart_assistant/memory/memory_store.py` (改)
- **修改内容**: (1) M17: 添加 `_dirty` flag，`_flush` 仅在 dirty 时执行 I/O；(2) m4: `_access_order: list` 改为 `OrderedDict`，`_update_lru` 使用 `move_to_end()` O(1)，`_evict_lru` 使用 `popitem(last=False)` O(1)；(3) m8: `MemoryWriterThread` 添加完整类 docstring；(4) m35: `_flush` 异常时 `os.unlink(tmp_meta)` 清理临时文件
- **原因**: 消除 0.5s 不必要唤醒、LRU O(n)→O(1)、资源泄漏修复

### `src/transbridge/smart_assistant/conversation_manager.py` (改)
- **修改内容**: (1) M3: 添加 `_turn_starts: list[int]`，`add_user()` 记录轮次起始位置，`_trim()` 基于记录位置裁剪；(2) m5: 添加 `_messages_dirty` flag + `_messages_cache`，仅在变更时重建副本；(3) m6: `_trim()` 改为列表切片 `self._messages[oldest_end:]` 替代逐条 del
- **原因**: 轮次界定更鲁棒、减少 O(n) 内存拷贝、O(n*k)→O(n) 裁剪

### `src/transbridge/smart_assistant/context_builder.py` (改)
- **修改内容**: (1) M21: translated 计数和分类分布合并为单次遍历；(2) m4: `__init__` 的 `ctx` 参数添加 `ctx: Any | None = None` 类型标注
- **原因**: 消除双重遍历、补齐类型信息

---

## Agent-4: tools/ 子包 (15项)

### `src/transbridge/smart_assistant/tools/base.py` (改)
- **修改内容**: M1: `execute_with_guardrails` 的 dict 分支构造 `ToolResult` 后经过 after 中间件链（OutputValidationGuard 脱敏/截断），不再裸返回 dict
- **原因**: v1 迁移中返回 dict 的工具可能绕过安全护栏

### `src/transbridge/smart_assistant/tool_registry.py` (改)
- **修改内容**: M2: `list_all()` 新增 `include_deprecated: bool = False` 参数，默认排除 deprecated 工具
- **原因**: MCP 通道通过 `list_all()` 暴露工具列表，应默认隐藏已废弃工具

### `src/transbridge/smart_assistant/tools/tool_translator.py` (改)
- **修改内容**: (1) M4: 线程启动前浅拷贝 `collection` 和 `entry_ids`；(2) M27: 提取 `_get_post_process_config()` + `_get_term_db_info()` 辅助函数，主函数从 65 行缩减为 35 行；(3) m25: `_tool_get_scope_preview` 改为调用 `filter_entries`
- **原因**: 闭包可变引用风险、长函数拆分、筛选逻辑统一

### `src/transbridge/smart_assistant/tools/tool_writer.py` (改)
- **修改内容**: (1) M6: `_tool_write_to_strings` 添加路径必填检查；(2) m34: 添加 NOTE 注释说明与 tool_parser 的路径校验重复及不合并理由
- **原因**: 路径校验形同虚设修复

### `src/transbridge/smart_assistant/tools/tool_v1.py` (改)
- **修改内容**: m31: `_tool_export_json` 默认路径也通过 `_validate_output_path` 校验
- **原因**: stem 可能包含路径遍历字符

### `src/transbridge/smart_assistant/tools/tool_proofreader.py` (改)
- **修改内容**: m6: `type('Cfg', (), {})()` 替换为 `SimpleNamespace()`
- **原因**: 标准库方案比动态类创建更清晰

### `src/transbridge/smart_assistant/tools/tool_editor.py` (改)
- **修改内容**: (1) M9: 添加 NOTE 注释说明 batch set_stage 无批处理优化；(2) m28: `_tool_list_labels` 区分"未初始化"和"空标签库"
- **原因**: 已知限制文档化、更精确的错误消息

### `src/transbridge/smart_assistant/tools/tool_paratranz.py` (改)
- **修改内容**: M10: 添加 NOTE 注释说明全量同步上传无批处理的已知限制
- **原因**: 已知限制文档化

### `src/transbridge/smart_assistant/tools/tool_default.py` (改)
- **修改内容**: m10: 确认 `list_local_projects` 已脱敏（仅返回 `p.name`）
- **原因**: 安全审计确认

---

## Agent-5: guardrails/obs/mcp/panel/markdown/skills (14项)

### `src/transbridge/smart_assistant/guardrails/output_validator.py` (改)
- **修改内容**: (1) M13: 类型检查从 `isinstance(data, dict)` 放宽为 `isinstance(data, (dict, list))`；(2) M19: `_redact_dict` / `_redact_list` 改为就地变异（mutate in place），仅 tuple 需转换时新建
- **原因**: 扩展性提升、大数据量下性能优化

### `src/transbridge/smart_assistant/guardrails/input_validator.py` (改)
- **修改内容**: M10: `_detect_path_traversal` 从硬编码 9 个 path_keys 改为启发式检测（参数名含 path/file/dir/output/dest/save 子串）
- **原因**: 新工具可能使用不同路径参数名绕过检测

### `src/transbridge/smart_assistant/guardrails/permission.py` (改)
- **修改内容**: M8: 添加注释说明 PermissionGuard 仅返回状态，UI 弹窗由上层触发
- **原因**: 职责边界文档化

### `src/transbridge/smart_assistant/mcp/adapter.py` (改)
- **修改内容**: M11: `_is_exposed` 改为完整 docstring，添加 M11 TODO 注释说明 admin_tool_whitelist 无法绕过 PermissionGuard
- **原因**: MCP 不支持 HITL 确认，配置误导问题文档化

### `src/transbridge/smart_assistant/mcp/server.py` (改)
- **修改内容**: (1) m3: 添加 `_MAX_LINE_LENGTH = 10MB` 检查；(2) m36: `req_token == auth_token` 改为 `hmac.compare_digest()`
- **原因**: DoS 防护、时序攻击面消除

### `src/transbridge/smart_assistant/observability/collector.py` (改)
- **修改内容**: (1) M16: `_save_trace()` 包装在 `threading.Thread` 中异步执行；(2) m9: `_cleanup_old` 添加 `_MAX_CLEANUP_FILES = 500` 上限；(3) 添加 `_MAX_TRACE_AGE_DAYS = 30` 类常量
- **原因**: 主线程 I/O 阻塞消除、文件数爆炸防护

### `src/transbridge/ui/tools/smart_assistant/panel.py` (改)
- **修改内容**: (1) M24: `__init__` 拆分为 `_init_skills()` + `_init_ui(ctx)`；(2) m3: `closeEvent` 添加 `end_conversation()` 调用确保 observability trace 被保存
- **原因**: 构造函数瘦身、观测数据不丢失

### `src/transbridge/infra/markdown_renderer.py` (改)
- **修改内容**: M9: 链接替换改为 `_LINK_RE.sub(_sanitize_link_url, text)`，新增 `_sanitize_link_url()` 校验 URL 协议，仅允许 `http:`/`https:`/`#`/相对路径，`javascript:`/`data:` 等替换为 `about:blank`
- **原因**: XSS 防护 — LLM 返回内容中可能含恶意协议链接

### `src/transbridge/smart_assistant/skills/skill_loader.py` (改)
- **修改内容**: m39: 添加 `MAX_PROMPT_TEMPLATE_LENGTH = 4096`，超长时截断并 warning
- **原因**: 恶意 TOML 可注入超长 prompt 引发 token 浪费

### `src/transbridge/smart_assistant/skills/skill_executor.py` (改)
- **修改内容**: m22: 更新 `execute()` docstring 说明异步特性（LLM 在后台 QThread 处理，方法立即返回）
- **原因**: 异步行为文档化

---

## Agent-6: config + 类型标注 + 硬编码 (12项)

### `src/transbridge/config/llm.py` (改)
- **修改内容**: M25: 新增 `_CONFIG_FIELDS` / `_EMBEDDING_FIELDS` / `_GUARDRAILS_FIELDS` / `_MCP_FIELDS` 配置描述表 + `_ensure_section()` 辅助方法，`save_to_file()` 和 `load_from_file()` 重写为表驱动循环，样板代码减少 ~60%
- **原因**: 消除高度重复的逐字段 set/get 样板代码

### `src/transbridge/smart_assistant/tool_registry.py` (改)
- **修改内容**: m16: `_ToolRegistry` 类添加注释说明 `_` 前缀是历史惯例，`ToolRegistry = _ToolRegistry` 别名对外暴露
- **原因**: 命名约定文档化

### `src/transbridge/smart_assistant/conversation_manager.py` (改)
- **修改内容**: m17: 所有方法添加完整类型标注 (`list[dict[str, Any]]`、`-> None` 等)，`_MAX_OBSERVATION_CHARS = 2000` 提取为类常量
- **原因**: 类型安全提升

### `src/transbridge/smart_assistant/agents/orchestrator.py` (改)
- **修改内容**: m18: `decompose_task` 和 `map_to_steps` 的 `ctx` 参数添加 `ctx: Any` 类型标注
- **原因**: 类型信息补全

### `src/transbridge/smart_assistant/guardrails/base.py` (改)
- **修改内容**: m20: `before_execute` / `after_execute` 的 `ctx` 参数添加 `ctx: ExecutionContext` 前向引用类型，添加 docstring
- **原因**: ABC 接口类型信息补全

### `src/transbridge/ui/tools/smart_assistant/message_bubble.py` (改)
- **修改内容**: (1) m26: `_RENDERER` 模块级预实例化改为 `_get_renderer()` 延迟初始化；(2) m24: 移除 stylesheet 中无效 `padding: 0px`；(3) `_BUBBLE_MAX_WIDTH = 420` 提取为类常量
- **原因**: 导入顺序依赖消除、CSS 清理

### `src/transbridge/ui/tools/smart_assistant/tool_card.py` (改)
- **修改内容**: m30: `set_result()` 添加 NOTE 注释说明按钮禁用后由调用方负责追加消息
- **原因**: 行为契约文档化

### 硬编码提取（多文件）
- **修改内容**: `_STREAMING_FLUSH_MS = 50` (chat_widget)、`_SAFE_SERIALIZE_MAX_CHARS = 200` (execution_engine)、`_MAX_TRACE_AGE_DAYS = 30` (observability)、`_MAX_OBSERVATION_CHARS = 2000` (conversation_manager)、`_BUBBLE_MAX_WIDTH = 420` (message_bubble)
- **原因**: 魔法数字消除，提升可配置性和可读性
