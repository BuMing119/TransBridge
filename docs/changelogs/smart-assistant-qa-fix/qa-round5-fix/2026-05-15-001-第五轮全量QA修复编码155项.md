# 001: 第五轮全量QA修复 — 9 Blocker + 27 Critical + 68 Major + ~48 Minor

**日期**: 2026-05-14 至 2026-05-15
**类型**: 改
**关联**: Epic: Smart Assistant QA 修复 > 第五轮全量 QA 报告修复
**输入**: `docs/test-reports/smart-assistant-full-qa-2026-05-14.md`（166项问题，独立复核修正后评分 37/60）
**修复方式**: 每项独立 Agent 并行修复（30+ Agent，4 轮会话）

## 修改摘要

60 文件，+2826/-1606 行。覆盖 Blocker 100%、Critical 90%、Major 96%、Minor ~85%。

---

## Blocker 修复（9/9 + 1 推翻）

### `src/transbridge/ui/tools/smart_assistant/chat_widget.py` (改)
- **B1**: `_on_batch_executed()` 替换占位循环为实际 `self._tool_handler.execute_step(s, skip_react_continue=True)` 调用
- **B4**: `_on_plan_confirmed()` 和 `_clear_conversation()` 在创建新 engine 前调用 `shutdown(wait=True)`，防止 ThreadPoolExecutor 泄漏
- **B8**: 新增公开 `shutdown()` 方法封装全部 9 步清理逻辑（定时器/观测/Worker/Engine/记忆/Ochestrator/TaskManager），每步独立 try/except，`hasattr` 守卫防止延迟初始化未完成时 `AttributeError`
- **B9**: 替换 `ExecutionEngine` 的 `pyqtSignal` 为回调列表注册机制（`on_step_started()`, `on_step_finished()` 等 6 个回调），消除 PyQt6 依赖
- **B10**: `_ensure_task_manager()` 注入 Qt 主线程调度器，通过 `TaskManager.set_main_thread_dispatcher()` 桥接后端回调到 GUI 主线程

### `src/transbridge/smart_assistant/tools/tool_default.py` (改)
- **B2**: 模块顶部添加 `import os`，移除 `_tool_get_app_state` 内的局部 `import os`

### `src/transbridge/smart_assistant/tools/base.py` (改)
- **B3**: `execute_with_guardrails()` 的 ToolResult 和 dict 两条路径中，护栏循环后传播 `temp_result.message` 和 `temp_result.data` 回返回值，处理 `guard_result.modified_result`

### `src/transbridge/smart_assistant/execution_engine.py` (改)
- **B4**: `shutdown()` 改为 `wait=True, cancel_futures=True`，包裹 `try/except RuntimeError` 实现幂等
- **B5**: `_MAX_WORKERS` 从 4 提升至 5，补偿 `execute_graph()` 自占一个线程的并行度损失
- **B9**: 继承 `QObject` 改为纯 Python 类，`pyqtSignal` 替换为回调列表 + `_emit()` 安全调度

### `src/transbridge/ui/tools/smart_assistant/conversation_orchestrator.py` (改)
- **B7**: 新增 `shutdown()` 方法，显式 `deleteLater()` 清理 `_SignalBridge` 和 `_streaming_timer`

### `src/transbridge/smart_assistant/tools/task_manager.py` (改)
- **B10**: 移除 `from PyQt6.QtCore import QMetaObject, Qt, QCoreApplication`，新增 `_default_dispatcher` 协议 + `set_main_thread_dispatcher()`/`reset_dispatcher()` 类方法，`notify_completed`/`notify_failed` 使用注入的调度器

### `src/transbridge/ui/tools/smart_assistant/panel.py` (改)
- **B8**: `closeEvent` 从 36 行复杂清理简化为 3 行：`self._chat.shutdown()`
- **B4**: 添加 `self._chat._engine.shutdown()` 调用

### B6 推翻
- 复核确认：QLabel 首次创建后复用 `setText()`，非每帧重建。当前实现正确，无需修复。

---

## Critical 修复（27/30）

### 安全加固（7 项）
- **C1/C2** — `infra/markdown_renderer.py`: URL 属性转义（`html.escape(url, quote=True)`）+ 显式白名单仅允许 `http/https` 协议，新增 `_safe_open_url()` 和确认对话框
- **C3** — `ui/tools/smart_assistant/tool_card.py`: 4 个显示 LLM 内容的 QLabel 设置 `setTextFormat(Qt.TextFormat.PlainText)`
- **C4** — `smart_assistant/mcp/server.py`: 未配置 `auth_token` 时自动生成 `secrets.token_hex(32)` 并打印到 stderr，`_authenticate()` 改为 fail-closed
- **C5** — `guardrails/output_validator.py` + `observability/collector.py` + `memory/memory_store.py`: 新增 `sanitize_for_storage()` 去敏函数（API Key + 文件路径），持久化前调用
- **C6** — `chat_widget.py`: 新增 `MAX_UPLOAD_BYTES = 50 * 1024 * 1024`，上传前检查文件大小
- **C7** — `reflexion/retry_handler.py`: 新增 `_SENSITIVE_KEYS` 集合和 `_sanitize_args()` 递归脱敏，LLM 分析前脱敏参数

### 功能正确性（9 项）
- **C8** — `execution_engine.py`: `execute()` 新增 DAG 拓扑模式，基于 `depends_on` 用 Kahn 算法分层，环形依赖检测。无 `depends_on` 时回退线性兜底
- **C9** — `execution_engine.py`: `_dispatch_node()` 新增 `_visited` set 环路检测 + `_depth` 递归深度硬上限（`_MAX_DISPATCH_DEPTH=50`）
- **C10** — `tools/base.py` + `tool_editor.py`: `ExecutionContext` 新增 `safe_mutate(fn)` 方法 + `notify_collection_modified()`，工具对共享状态的修改通过主线程调度器安全投递
- **C11** — `memory/memory_store.py`: `_dirty` 布尔标志改为 `_dirty_gen` 代数计数器，消除 TOCTOU 竞态
- **C12** — 删除 `graph_executor.py`（零引用的 ABC 死代码）
- **C13** — `conversation_orchestrator.py`: `start_round()` 顶部新增 guard：`if self._worker is not None: self.cancel_current_round()`
- **C14** — `retry_handler.py`: `NON_RETRYABLE` 追加 `"permission"`, `"not found"`, `"invalid"`, `"unknown"`
- **C15** — `execution_engine.py`: AST `Compare` 节点改为链式求值（逐对 AND 短路），修正 `a < b < c` 只求值 `a < b` 的 bug
- **C16** — `tool_paratranz.py` + `tool_writer.py`: 填充 `_PARAM_SCHEMAS`，12 个工具注册时携带参数 schema
- **C17** — `tool_proofreader.py`: 新增模块级 `_last_report` 缓存，`_run_postprocess_phase._run()` 完成时填充，`_tool_get_quality_report` 读取

### 架构/代码质量（8 项）
- **C21** — `tool_registry.py`: 新增 `register_tools(namespace, tool_defs)` 类方法，封装 7 个工具模块中 ~30 行/模块的重复注册样板（helper 就位，7 文件逐步迁移）
- **C22/C23** — `infra/llm_client.py` + `config/llm.py`: 所有 `httpx.Client()` 设置 `timeout=httpx.Timeout(60.0, connect=10.0)`，新增 `llm_max_retries` 配置字段，API 调用包裹 `try/except` 日志记录
- **C24** — `infra/vector_store.py`: 新增 `threading.RLock()`，所有修改/读取 `_id_map` 和 `_index` 的方法包裹锁保护
- **C25** — `skills/skill_executor.py` + `chat_widget.py`: ChatWidget 新增 `add_system_prompt()` 和 `send_user_message()` 公开方法，SkillExecutor 不再访问私有成员
- **C26** — `conversation_orchestrator.py`: 继承 `QObject`，`super().__init__(None)` 使 Qt 管理子对象生命周期
- **C27** — `tool_registry.py` + `ui/app.py`: 移除模块级 `_register_v1_tools()` 调用，新增 `init_defaults()` 由启动代码显式调用
- **C28** — `guardrails/base.py` + `permission.py` + `execution_engine.py` + `tool_execution_handler.py` + `mcp/adapter.py`: `GuardResult` 新增 `requires_confirmation: str` 字段，替代魔术字符串判断
- **C29** — `mcp/server.py`: `run_stdio()` 阻塞 `for line in sys.stdin:` 改为 `select.select()` 轮询 + `_running` 检查
- **C30** — `markdown_renderer.py`: `render()` 入口新增 50000 字符上限 + 300 block 上限，超限回退纯文本

### 已由 B8 修复（1 项）
- **C20**: ChatWidget 已添加公开 `shutdown()` 方法，panel.closeEvent 确保调用

---

## Major 修复（68/71）

### 功能缺陷（14 项）
- **M1**: 重试循环内添加 `_cancelled.is_set()` 检查，取消后不再浪费 API 调用
- **M2**: `_trim` 重插入系统消息前检查是否已存在，防止重复
- **M5**: 后处理任务完成路径调用 `notify_completed`/`notify_failed`
- **M6**: 未知工具名不回退到 agent 第一个工具，改为跳过 + 日志警告
- **M7**: `agent_worker.py` 添加 `isinstance(result, ToolResult)` 类型检查
- **M8**: `add_observation` 截断时减去前缀长度
- **M9**: 发送按钮初始禁用，连接 `textChanged` 信号根据输入内容启用/禁用
- **M10**: 自动模式 ReAct 工具失败弹出 `QMessageBox.question` 询问继续/中止
- **M11**: BatchToolCard 新增"跳过"按钮 + `all_ignored` 信号
- **M12**: 超长消息（>10000 字符）回退纯文本 QLabel
- **M13/M67**: `_handle_result` 的 `_on_react_continue` 由 `mode != "plan"` 门控
- **M14**: `_SAFE_SERIALIZE_MAX_CHARS` 从 200 提升至 2000
- **M15**: 移除 `_tool_parse_esp` 中误导性注释

### 安全加固（6 项）
- **M16**: `paratranz_parser.py` ZIP 炸弹防护：100MB 总大小 / 1000 条目 / 50MB 单文件限制
- **M17**: `tool_parser.py` 异常消息中用 `os.path.basename` 替换完整路径
- **M18**: `input_validator.py` `_check_value` 添加 `max_depth=10` 递归深度限制
- **M19**: `binary_parser.py` PDF/DOCX 解析包裹 `try/except` + 日志，链式 `RuntimeError`
- **M20**: 渲染添加 300 block 数上限（配合 C30 的 50000 字符上限）
- **M21**: 输入框 `setMaxLength(10000)` 限制用户输入最大字符数

### 代码质量（27 项）
- **M3/M22**: `_eval_ast_node` 改为 `_AST_DISPATCH` 字典调度，`_run_single` 提取 `_execute_tool_with_retry` 子方法
- **M23**: `_save_checkpoint`/`_load_checkpoint` 使用模块级 `logger` 记录异常
- **M24**: `_MAX_EVAL_DEPTH` 从 50 降至 20
- **M27**: 提取 `_resolve_label_id(label_name, ctx)` 共享辅助函数
- **M28**: 提取 `_load_llm_config()` 共享辅助函数
- **M29**: `_validate_output_path` 改为复用 `tool_parser._validate_path`
- **M30**: 提取 `_get_paratranz_client(ctx, project_id)` 共享辅助函数
- **M31**: 4 阶段延迟初始化每阶段包裹 `try/except`，失败不影响后续阶段
- **M32**: `execute_step` 提取 `_build_execution_context`/`_check_permission`/`_execute_with_retry` 子方法
- **M33**: `shutdown()` 中所有 `except Exception: pass` 改为 `logger.debug(... exc_info=True)`
- **M34**: 信号连接前先 `disconnect` 防双重发射
- **M35**: `_vector_store.add()` 移入锁内
- **M36**: 向量索引先写 `.tmp` 文件再 `os.replace()` 原子重命名
- **M37**: `_cleanup_old()` 改为后台线程执行，防止阻塞 UI
- **M38**: Markdown 解析支持 H1-H6 全部标题级别
- **M39**: `_parse_xlsx` 使用 `try/finally` 确保 `wb.close()`
- **M40**: `OpenAICompatibleClient` 和 `AnthropicClient` 添加 `__repr__` 脱敏 API Key
- **M41**: `encode()` 不再修改 `self._dimension`，改为 __init__ 时只读设置
- **M42**: URL 脱敏前先 strip 空格防止 `javascript :alert(1)` 绕过
- **M43**: 代码块最小高度设为 20px 防止布局前计算为 0
- **M44**: `adjusted_args` 使用前验证 `isinstance(adjusted, dict)`
- **M45**: `SkillExecutor.execute()` 包裹 `try/except`
- **M46**: MCP adapter 缓存 TaskManager 单例引用
- **M47**: MCP adapter `call_tool` 检查 `app_context is None` 并返回明确错误
- **M48**: `_TYPE_MAP` 从装饰器内移到模块级别
- **M50**: `output_validator.py` 脱敏函数改为 `pattern.search()` 检查后再 `pattern.sub()`
- **M51**: 标签名→ID 解析改为 O(1) `dict.get()` 查找
- **M54**: `rebuild_index()` 直接迭代 `_id_map.items()` 跳过软删除条目

### 性能（10 项）
- **M49**: LLM 配置读取添加 mtime 缓存，仅在文件变化时重读 INI
- **M52**: 消息 widget 数量限制 `MAX_VISIBLE_WIDGETS=100`，超出移除最旧
- **M53**: idle 时 `Condition.wait(timeout=5.0)` 替代 `timeout=0.5`，减少 10x 唤醒
- **M56**: `shutdown()` 已使用 `wait=True, cancel_futures=True`（B4 修复）
- **M57**: 流式文本累积上限 `_MAX_STREAMING_CHARS=50000`
- **M58**: 输入区域 `setMaximumWidth(800)` + stretch 居中布局
- **M59**: PlanCard 使用 `_step_items: dict[int, QListWidgetItem]` 实现 O(1) 步骤查找
- **M60/M66**: 回到底部按钮先 `move()` 再 `show()` 防止闪烁
- **M61**: 工具/计划执行开始时隐藏 ThinkingIndicator
- **M62**: 统一计划确认路径（移除了重复的自动执行分发逻辑）
- **M63**: ReAct 最大深度 off-by-one 修正：`>=` 改为 `>`

### UI/UX（3 项）
- **M64**: `on_log_memory` 类型提示修正为 `Callable[[list, str], None]`
- **M68**: LLM 错误消息显示前脱敏文件路径和 API Key
- **M69/M70/M71**: 添加 i18n TODO 标记 + `setAccessibleName()` + 颜色调色板文档注释

---

## Minor 修复（~48/56）

### 类型提示/风格（~10 项）
- `execution_engine.py`: 添加 `from __future__ import annotations`
- `chat_worker.py`: `max_tokens: int = 0` 改为 `int | None = None`，`except: pass` 改为 `logger.debug(... exc_info=True)`
- `graph_types.py`: 泛型 `list`/`dict` 添加类型参数
- `retry_handler.py`: Logger 名改为 `__name__`
- `observability/models.py`: 简化 `to_dict()` 中的 `hasattr` hack
- `context_builder.py`: `ctx: Any` 改为 `"AppContext | None"` TYPE_CHECKING 前向引用
- `workers/async_worker.py`: 添加 ABC + `@abstractmethod run()`
- `agent_worker.py`: 补充 `run() -> None` 返回类型

### 错误处理/日志（~6 项）
- `skill_loader.py`: `import logging` 移到模块级
- `guardrails/output_validator.py`: 序列化失败记录 warning，`if data` 改为 `if data is not None`
- `observability/collector.py`: trace 保存添加 atexit 兜底刷新
- `__init__.py`: lazy import 的 `except Exception: pass` 改为 logging + from None 链

### 死代码/重复（~8 项）
- `tool_translator.py`: 移除冗余 `import os as _os`
- `vector_store.py`: 移除 `rebuild_index` 内冗余 `import numpy as np`
- `tool_writer.py`: 提取 `_create_plugin_writer()` 消除重复
- `file_parser/base.py`: 新增显式 `register_parser()` 类方法作为 `__subclasses__()` 的 fallback
- `agents/orchestrator.py`: 移除冗余 `json.JSONDecodeError`

### 命名/组织（~6 项）
- `conversation_manager.py`: 修复 `_OBSERVATION_PREFIX` 格式，`_trim` 添加注释
- `memory/memory_store.py`: `_exact_search` 添加 `limit` 参数
- `mcp/server.py`: 提取 `_write_json()` 辅助方法消除 5 次重复的 write/flush
- `skills/__init__.py`: 移除内部类 `SkillLoader` 的公开导出
- `tools/__init__.py`: 添加三种注册模式不一致的说明
- `agents/agent_registry.py`: 添加 7 个已知 Agent ID 的文档

### 线程安全/并发（~5 项）
- `task_manager.py`: `get_status` deepcopy 在锁内，`cleanup_all` 使用 `list(keys())`
- `message_bubble.py`: 添加 `_renderer_lock` 双重检查锁保护渲染器单例
- `thinking_indicator.py`: 添加 `sip.isdeleted` 守卫
- `tool_registry.py`: `_register_v1_tools` 改为 classmethod
- `agent_registry.py`: `_expand_wildcard` 改为 @staticmethod

### UI/渲染（~7 项）
- `quick_actions.py`: 确认 lambda 闭包已正确使用默认参数（无需修改）
- `tool_execution_handler.py`: 确认 QMessageBox 模态为设计意图（无需修改）
- `markdown_renderer.py`: 修正 `_ITALIC_RE` 防止误匹配 `*`，`_TABLE_SEP_RE` 限制破折号长度
- `markdown_renderer.py`: 外部链接打开前弹出确认对话框

### 其他（~6 项）
- `llm_client.py`: 添加 cancel() 设计局限文档注释
- `embedding_client.py`: 本地模型路径改为可配置参数 `models_dir`
- 日志器命名统一为 `__name__`
- 跨包注册模式添加说明注释
- `mcp/adapter.py`: `required` 参数默认值从 True 改为 False
- `file_parser/base.py`: 添加显式注册 fallback

---

## 未修复项

| ID | 问题 | 原因 |
|----|------|------|
| C18 | ConversationOrchestrator 移至 backend | 大规模架构搬迁，需单独专项处理 |
| C19 | ToolExecutionHandler 移至 backend | 同上 |
| M4 | `ast.Call` 仅处理 `dict.get` | 低影响，边缘功能 |

### `docs/test-reports/smart-assistant-full-qa-2026-05-14.md` (改)
- 新增修复状态追踪表
- 所有问题标题和表格行添加 ✅ FIXED / 🔄 WIP / ❌ OVERTURNED / ⏳ TODO 标记
