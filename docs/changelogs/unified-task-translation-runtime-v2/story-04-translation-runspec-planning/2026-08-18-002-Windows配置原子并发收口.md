# Story 04：Windows 配置原子并发收口

- 日期：2026-08-18
- 状态：综合 QA 通过

## 增量

- `ConfigRepository` 的进程内锁键改为稳定的绝对、大小写折叠路径，避免目标父目录创建前后产生不同锁身份。
- 原子替换对 Windows WinError 5/32 做有界重试；其他异常仍立即失败，且不降级为非原子写入。
- 增加确定性分享冲突测试，并将并发 section 更新与替换重试连续复验 10 轮。

## 证据

- 锁定 uv：47 passed。
- EvidenceManifest：`qa-evidence/task-s04/qa-20260818T131518.084476Z-b513ef423500/manifest.json`，verify 为 `VALID (passed)`。

