# Story 06：生产后处理单提交与结果保真

- 日期：2026-08-18
- 状态：综合 QA 通过

## 增量

- GUI/Agent 后处理入口改用不可变候选 workload、canonical report 与唯一 `CommitTranslations` 提交，不再由 legacy checker 直接修改正式 Collection。
- AutoTranslator 内建后处理复用同一执行服务和独立 checkpoint 身份。
- 合并 report/commit outcome 时保留 `PARTIAL`、`FAILED`、`CANCELLED`，避免部分结果在提交成功后被误报为 completed。

## 证据

- 锁定 uv：72 passed。
- EvidenceManifest：`qa-evidence/task-s06/qa-20260818T125309.913470Z-4d4ed7c14d10/manifest.json`，verify 为 `VALID (passed)`。

