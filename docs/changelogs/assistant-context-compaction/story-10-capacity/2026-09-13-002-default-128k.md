# 002：默认助手上下文容量提高到 128K

- 日期：2026-09-13。
- Epic / Story：assistant-context-compaction / S10。
- 用户要求：默认至少 128K，不再对未知模型使用 32K 默认容量。
- 关联：[计划](../../../../plans/assistant-context-compaction/plan.md)、[上一增量](2026-09-13-001-capacity-and-control-feedback.md)。

## 本次差异

- `src/transbridge/smart_assistant/context_capacity.py`：统一默认常量改为 131,072，未知服务来源显示“默认 128K”；已核验官方 V4 的 1M 及手动覆盖规则不变。
- `src/transbridge/smart_assistant/context_budget.py`：独立预算对象的默认值复用同一容量常量，避免另留 32K 隐藏默认。
- `tests/smart_assistant/test_context_capacity.py`：更新未知端点断言，增加默认预算与未知服务均可容纳超过 50,000 估算 token 的回归；显式 32K 限制仍验证有效。
- `tests/ui/test_ui_settings_dialog.py`：验证未知端点显示默认 128K 和 131,072。
- `docs/adr/041-assistant-context-compaction.md`、`plans/assistant-context-compaction/plan.md`：在现行条款中同步用户指定默认值。
- `plans/INDEX.md`、`docs/changelogs/INDEX.md`：更新本 Epic 最新增量链接，保留同期其他工作内容。

## 验证

- `uv run pytest tests/smart_assistant/test_context_capacity.py tests/smart_assistant/test_request_context_budget.py tests/smart_assistant/test_conversation_orchestrator_lifecycle.py tests/application/assistant_context tests/config/test_assistant_context_settings.py tests/ui/test_ui_settings_dialog.py tests/ui/tools/smart_assistant/test_request_lifecycle_panel.py -q`：165 passed，3 条既有弃用警告，14.43 秒。
- `uv run ruff check src tests`：通过。
- `uv run ruff format --check src tests`：通过，1,356 文件符合格式。
- `git diff --check`：通过。

只验证默认策略及相关链路，未重复全仓库测试，未调用真实模型，未修改用户配置或提交代码。
128K 是未知服务的应用默认值，不声明服务端保证支持；已有正数配置原样保留，可在设置中显式切换自动。
本轮无新增临时文件，上一增量保持历史原文。
