# Translation I/O Kernel V2 / Story 06：原子发布、验证与备份

- **日期**：2026-08-18
- **状态**：已实现并增量验证通过
- **追溯**：FR18.7、NFR2.1、NFR3.1；ADR-017；R-018

## 变更

- 新增 PublishCoordinator、可注入 filesystem、验证器与发布证据 DTO；staging 固定目标同目录、限制权限，并在 replace 前执行摘要、目标指纹、取消与 commit permit 门禁。
- EET、XT、Localized Strings 采用真实 parse→write→reparse/fidelity 成功链；失败、取消、fsync、replace、权限和磁盘故障均不删除既有目标。
- 目标冲突支持明确 overwrite 或 verified backup；UNC/cross-volume 能力不足拒绝发布，ESP partial fixture 继续 fail-closed。

## 验证

- 锁定 uv 集成及合同套件：127 passed、1 skipped（Windows symlink 权限）。
- [EvidenceManifest](../../../test-reports/requirement-code-review-2026-08-18/qa-evidence/io-s06/qa-20260818T090452.960541Z-c71a10145b91/manifest.json) 经 verify 通过；Ruff check/format 与定向 diff-check 通过。

## 已知边界

- 映射网络盘不能仅靠路径可靠识别；ESP 的现有 fixture 为 partial，须以 clean corpus 后的综合 QA 才能提升发布能力。
