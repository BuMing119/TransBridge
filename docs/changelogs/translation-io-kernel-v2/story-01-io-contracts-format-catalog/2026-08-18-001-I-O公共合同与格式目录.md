# Story 01：I/O 公共合同与格式目录

- 日期：2026-08-18
- Epic/Story：`translation-io-kernel-v2/S01`
- 追溯：FR18.1/18.2/18.8、ADR-017、R-013/R-014/R-019
- 状态：实现完成，增量验证通过；待综合 QA

## 实现增量

- 新增无 GUI/具体 parser 依赖的 `FormatAdapter`、Parse/Write/Probe request/result、SourceDescriptor/SourceSnapshot、取消令牌和能力合同。
- 明确 `plugin.sse`、EET XML/Binary、XT XML、ParaTranz/DSD/TransBridge JSON、SSU8/SSU9、STRINGS/DLSTRINGS/ILSTRINGS 共 12 个格式 ID。
- `FormatCatalog` 以 magic、XML root、JSON schema 和 Localized Strings 结构证据区分 exact/ambiguous/unsupported；扩展名只提供 hint，显式 hint 与强证据冲突产生结构化诊断。
- 合法空 completed、partial、failed、cancelled 严格互斥；失败或取消不携带可发布 SourceSnapshot。
- capability snapshot 同时记录 adapter 声明和架构 policy ceiling；未注册 V2 adapter 默认 unavailable，SST read 最高 experimental、write/round-trip/publish 永远 unavailable。
- 删除测试子目录的 `__init__.py`，避免 `tests/contracts/io` 被 Python 解析成与标准库 `io` 冲突的顶层包。

## 验证证据

- `uv run --locked python -m pytest tests/contracts/io -q -p no:cacheprovider`：23 passed。
- `uv run --locked ruff check src/transbridge/application/io tests/contracts/io`：通过；Ruff format check：7 files already formatted。
- [EvidenceManifest](../../../test-reports/requirement-code-review-2026-08-18/qa-evidence/io-s01/qa-20260818T065601.384283Z-66d4e94e3798/manifest.json)：`passed`，schema/verdict/hash 复验有效。
- 未执行 Git commit/push。

## 剩余门禁

当前没有 V2 adapter 宣称 available；EntryKey/ChangeSet、ParaTranz 双 ID、真实 ESP/EET/XT adapter、Stage/Localized Strings 保真和原子发布分别由 S02～S06 承接。综合 QA 前不得以目录存在替代真实 parse→write→reparse 成功链。
