# 001: Story 02 编码实现 — TaskManager 线程安全强化

**日期**: 2026-05-11
**类型**: 增/改
**关联**: Epic: Agent 工具系统全面扩展 > Story 02: TaskManager

## 修改文件

### `src/transbridge/smart_assistant/tools/task_manager.py` (增)
- **修改内容**: 创建 TaskManager 单例（~125 行）。TaskHandle dataclass 含 stop_event/pause_event(预留=None)/status/progress/created_at/metadata/_thread(O9线程引用)。TaskManager 模块级 _instance_lock 双重检查锁(E3)。register() 支持传入 stop_event+metadata+thread 参数。cancel() 设置 stop_event 并更新状态。get_status() progress 深拷贝返回(E3)。update_progress() 合并更新进度。list_active() 过滤 status="running"(E7联动:无paused)。list_all() 返回所有任务。cleanup() 弹出句柄后 join 线程(timeout=5,O9)。cleanup_all() 批量清理非活跃任务
- **原因**: 长运行任务（翻译/后处理/ParaTranz）需要统一生命周期管理，线程安全强化防止并发竞态和内存泄漏

### `src/transbridge/smart_assistant/tools/__init__.py` (改)
- **修改内容**: 新增 TaskManager 导入和导出
- **原因**: 对外暴露 TaskManager 单例，供 execution_engine 和后续工具模块引用
