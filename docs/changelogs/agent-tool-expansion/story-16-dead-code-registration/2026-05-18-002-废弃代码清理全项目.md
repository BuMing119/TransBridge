# 002: 全面废弃代码清理 — 5文件 + v1命名空间

**日期**: 2026-05-18
**类型**: 删
**关联**: Epic: Agent 工具系统全面扩展 > Story 16: 死代码清理

## 修改文件

### `src/transbridge/smart_assistant/tools/tool_proofreader.py` (删)
- **修改内容**: 删除 5 个已废弃未注册的旧函数（`_tool_run_consistency_check` / `_tool_run_format_validation` / `_tool_run_llm_refinement` / `_tool_run_llm_polish` / `_tool_run_llm_arbitration`），每个约 6 行含 `warnings.warn` + `ToolResult.fail`。删除 `import warnings`
- **原因**: Story 25 统一为 `run_postprocess`，旧函数体已无用

### `src/transbridge/smart_assistant/tools/tool_writer.py` (删)
- **修改内容**: 删除 4 个已废弃未注册的旧 wrapper（`_tool_write_to_esp` / `_tool_write_to_eet` / `_tool_write_to_xt` / `_tool_write_to_strings`）和对应的 `_PARAM_SCHEMAS` 旧条目。删除 `import warnings`
- **原因**: Story 19 合并为 `write_back`，旧 wrapper 已无用

### `src/transbridge/smart_assistant/tools/tool_editor.py` (删)
- **修改内容**: 删除 9 个已废弃未注册的旧函数（`_tool_filter_by_stage` / `_tool_filter_by_category` / `_tool_filter_by_label` / `_tool_search_entries` / `_tool_clear_all_filters` / `_tool_create_label` / `_tool_assign_label` / `_tool_remove_label` / `_tool_batch_assign_label`）和 8 个对应的 `_PARAM_SCHEMAS` 旧条目。删除 `import warnings`。`_tool_get_visible_entries` 中的 tool_suggestions 从 `"search_entries"` 改为 `"set_filters"`
- **原因**: Stories 17/20 合并为 `set_filters` 和 `manage_entry_labels`，旧函数体已无用

### `src/transbridge/smart_assistant/tools/tool_translator.py` (删)
- **修改内容**: 删除 `_tool_stop_all_tasks` 废弃函数和 `import warnings`
- **原因**: Story 18 合并为 `stop_task`（不传 task_id 即停止全部），旧函数体已无用

### `src/transbridge/smart_assistant/tools/tool_v1.py` (删)
- **修改内容**: 整个文件内容清空（原 169 行），替换为简短 docstring 说明 v1 工具已废弃并列出替代工具
- **原因**: 5 个 v1 工具（`search_terms`/`translate_selected`/`check_quality`/`export_json`/`write_translations`）已被新工具系统完全替代

### `src/transbridge/smart_assistant/tool_registry.py` (删)
- **修改内容**: 删除 `_register_v1_tools` 类方法（~60 行，注册 5 个 v1 工具）
- **原因**: v1 工具已全部废弃，不再需要注册

### `src/transbridge/ui/app.py` (改)
- **修改内容**: 删除 `ToolRegistry.init_defaults()` 调用（3 行），不再初始化 v1 工具
- **原因**: v1 命名空间已废弃

### `tests/test_agent_tool_integration.py` (改)
- **修改内容**: 13 处测试用例从废弃函数调用改为新 API：`_tool_filter_by_stage`→`_tool_set_filters`、`_tool_search_entries`→`_tool_set_filters`、`_tool_clear_all_filters`→`_tool_set_filters({"clear":True})`、`_tool_create_label`/`assign`/`remove`/`batch_assign`→`_tool_manage_entry_labels`、`_tool_stop_all_tasks`→`_tool_stop_task`。`MockAppContext` 新增 `safe_mutate` 方法
- **原因**: 废弃函数已删除，测试需适配新 API。87/89 通过

### `docs/temp/batch3-writer-parser-tools.md` (改)
- **修改内容**: Parser 6 工具各新增 `action` 参数说明（`create_slot`/`append`），权限标注 write，返回字段更新，对比表重构
- **原因**: Story 24 副作用补全的文档同步

### `docs/temp/batch4-proofreader-paratranz-tools.md` (改)
- **修改内容**: Proofreader 从 6 工具（5个独立+1报告）改为 2 工具（`run_postprocess`+`get_quality_report`），ParaTranz 工具重新编号 3→11
- **原因**: Story 25 后处理统一的文档同步
