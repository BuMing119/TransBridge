# 001: Story 16 死代码清理与注册样板消除

**日期**: 2026-05-18
**类型**: 删/改
**关联**: Epic: Agent 工具系统全面扩展 > Story 16: Agent 死代码清理 + 注册样板消除

## 修改文件

### `src/transbridge/smart_assistant/agents/orchestrator.py` (删)
- **修改内容**: 删除整个文件（~120 行）。Orchestrator 类原本负责将 LLM action 字段映射到具体 tool_name，但该类从未在生产环境运行，被 `conversation_orchestrator.py` 中的 `ConversationOrchestrator` 替代
- **原因**: 死代码清理，该类从未在运行时被实际调用，属于早期 Route B Agent 调度方案的残留

### `src/transbridge/smart_assistant/agents/agent_worker.py` (删)
- **修改内容**: 删除整个文件（~72 行）。AgentWorker(AsyncWorker) 类原本用作 Agent 并行执行的 worker 线程基类
- **原因**: 与 Orchestrator 配套的死代码，从未在生产环境运行

### `src/transbridge/smart_assistant/agents/__init__.py` (改)
- **修改内容**: 移除 `from .orchestrator import Orchestrator` 和 `from .agent_worker import AgentWorker` 两行 import，`__all__` 从 `["AgentSpec", "AgentInstance", "AgentRegistry", "Orchestrator", "AgentWorker"]` 缩减为 `["AgentSpec", "AgentInstance", "AgentRegistry"]`
- **原因**: 随可执行文件删除，__init__.py 的导出同步清理

### `src/transbridge/smart_assistant/tools/tool_editor.py` (改)
- **修改内容**: `_register_editor_tools()` 函数重构——import 从 `ToolRegistry, ToolSpec` 简化为 `ToolRegistry`；将 14 个工具的元组列表 + for 循环改为单次 `ToolRegistry.register_tools("editor", [dict_list])` 调用。保留 `# Story 08: 标签管理` 注释，`require_confirmation` 仅 `batch_assign_label` 设为 True
- **原因**: 消除注册样板代码（Story 16 Phase 1a），7 模块统一使用 `register_tools()` 批量注册

### `src/transbridge/smart_assistant/tools/tool_translator.py` (改)
- **修改内容**: `_register_translator_tools()` 函数重构——import 移除 `ToolSpec`；9 个工具改为 `ToolRegistry.register_tools("translator", [...])` 调用。`is_long_running` 显式仅用于 start_translation/start_polish，`require_confirmation` 用于 start_translation/start_polish/stop_task，保留 `# Story 09: 翻译配置` 注释
- **原因**: 消除注册样板，统一使用 register_tools 批量注册

### `src/transbridge/smart_assistant/tools/tool_writer.py` (改)
- **修改内容**: `_register_writer_tools()` 函数重构——import 移除 `ToolSpec` 和 `# m10: ...` 注释；4 个 admin 级工具改为 `ToolRegistry.register_tools("writer", [...])` 调用，全部设置 `require_confirmation=True, is_long_running=True`
- **原因**: 消除注册样板，统一注册模式

### `src/transbridge/smart_assistant/tools/tool_parser.py` (改)
- **修改内容**: `_register_parser_tools()` 函数重构——import 移除 `ToolSpec` 和 `# m5: ...` 注释；6 个 read 级工具改为 `ToolRegistry.register_tools("parser", [...])` 调用，不显式指定 permission（使用默认 "read"）
- **原因**: 消除注册样板

### `src/transbridge/smart_assistant/tools/tool_proofreader.py` (改)
- **修改内容**: `_register_proofreader_tools()` 函数重构——import 移除 `ToolSpec`；6 个工具改为 `ToolRegistry.register_tools("proofreader", [...])` 调用。`run_llm_*` 三个工具显式设置 `is_long_running=True, require_confirmation=True, permission="write"`；其余 read 工具不显式指定额外字段
- **原因**: 消除注册样板，原代码通过 `startswith("run_llm_")` 动态判断标志位，现改为在 dict 中显式声明

### `src/transbridge/smart_assistant/tools/tool_paratranz.py` (改)
- **修改内容**: `_register_paratranz_tools()` 函数重构——import 移除 `ToolSpec`；10 个工具改为 `ToolRegistry.register_tools("paratranz", [...])` 调用。`is_long_running` 用于 upload_entries/download_entries/export_artifact，`require_confirmation` 用于 upload_entries/download_entries，保留 `# Story 15: 项目查询与切换` 注释
- **原因**: 消除注册样板

### `src/transbridge/smart_assistant/tools/tool_default.py` (改)
- **修改内容**: `_register_default_tools()` 函数重构——import 移除 `ToolSpec`；7 个工具改为 `ToolRegistry.register_tools("default", [...])` 调用。仅 switch_collection 为 `permission="write"`，其余 6 个为默认 "read"。保留 `# Story 12: 项目管理` 注释
- **原因**: 消除注册样板

## 测试结果

- 93 passed, 3 failed（全部为预存问题：MockAppContext.safe_mutate / UTF-8 编码 / _expand_wildcard 导入）
- 7 个 namespace 全部正确注册，非废弃工具总数保持 56 不变
- 净减 ~229 行（-194 死代码 + -35 注册样板）
