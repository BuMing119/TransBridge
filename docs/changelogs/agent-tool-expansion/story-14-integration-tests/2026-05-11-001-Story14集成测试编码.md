# 001: Story 14 集成测试编码 — 89 测试用例覆盖全链路

**日期**: 2026-05-11
**类型**: 增
**关联**: Epic: Agent 工具系统全面扩展 > Story 14: 跨 Story 集成测试

## 修改文件

### `tests/test_agent_tool_integration.py` (增)
- **修改内容**: 新建跨 Story 集成测试文件（~1060 行，89 测试用例），覆盖以下测试类：
  - `TestFullWorkflowChain` (17 用例): 筛选→搜索→查询→选择→编辑→标记端到端链路，验证 story 间数据传递正确性（filter_state → _selected_ids → entry.stage）
  - `TestLabelSystem` (3 用例): create→assign→batch→remove 标签全生命周期，验证 B1 标签数据通过 AppContext 共享
  - `TestSecurityGuardrails` (7 用例): 路径遍历拒绝（`../`/`..\\`/绝对路径 4 种）、权限拒绝、execute_with_guardrails 统一入口(B6)、输入校验拦截
  - `TestTranslationConfig` (4 用例): scope 设置/预览一致性、set_translation_config 不传 profile
  - `TestStateQueryTools` (4 用例): get_statistics/list_local_projects/get_current_project/get_quality_report
  - `TestParserWriterTools` (10 用例): _validate_path 文件存在+扩展名白名单(E1)+路径遍历、parse_esp 错误路径、writer admin 权限、parser read 权限(H6)
  - `TestToolResultV2` (9 用例): success/partial 语义(B3)、get()/__getitem__ 字典兼容(B2)、to_dict 序列化、truncated
  - `TestExecutionContext` (7 用例): __getattr__ 代理(H9)、collection/slot 属性转发、private attr 不代理、nonexistent 异常
  - `TestFilterEntries` (6 用例): 按 stage/category/search/combined 筛选(H8)、空 filter 返回全部
  - `TestTaskManager` (9 用例): 单例(E3)、深拷贝进度、cancel/cleanup、线程安全、list_all
  - `TestDecorators` (8 用例): @require_collection 成功/无集合/@validate_params 类型/缺失/非dict/装饰器顺序(E5)
  - `TestAgentRegistry` (6 用例): 7 Agent 注册、namespace 通配符展开(O3)、cross-namespace 工具、tool 已展开
  - MockAppContext 模拟真实 AppContext 的 ViewModel 层：filter_state/label_library/entry_labels/translation_scope/selected_ids/active_slot，含 collection 属性代理
- **原因**: Story 14 验收标准要求覆盖筛选→选择→翻译→标记完整链路、标签系统、安全护栏、翻译配置、parser/writer 权限等跨 Story 集成场景，确保 60 工具/7 Agent 的交互正确性
