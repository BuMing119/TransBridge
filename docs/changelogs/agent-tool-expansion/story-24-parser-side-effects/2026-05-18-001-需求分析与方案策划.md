# 001: FR9.12 需求分析与 Story 24 方案策划

**日期**: 2026-05-18
**类型**: 增
**关联**: Epic: Agent 工具系统全面扩展 > Story 24: Parser 工具副作用补全

## 修改文件

### `docs/requirements.md` (改)
- **修改内容**: 新增 FR9.12「解析工具副作用补全」需求条目（第 769 行前）。定义 6 个 Parser/Import 工具新增 `action` 参数（`create_slot`/`append`），指定 create_slot 创建并激活 CollectionSlot、append 追加条目到当前活跃集合。明确 HITL 确认机制、权限 write、无活跃集合时 append 返回 fail。将原 plan 中「集合管理 CRUD」部分移入范围（仅创建+追加）。
- **原因**: 当前 6 个工具仅解析文件返回 entry_count，解析结果被丢弃。用户期望 LLM 调工具后能真正落地解析结果，像 UI 手动操作一样。

### `plans/agent-tool-expansion/stories/story-24-parser-side-effects.md` (增)
- **修改内容**: 新建 Story 24 详细方案文档。定义 6 步实现：①重构工具函数保留解析结果 ②实现 `_create_slot()`/`_append_to_collection()` 副作用函数 ③接入 PermissionGuard HITL 确认（permission 改为 write）④更新 `_PARAM_SCHEMAS` 添加 action 参数 ⑤更新 plan.md 范围声明 ⑥验证。关键设计决策：同名 slot 冲突返回 fail（不新增 HITL 类型）、无集合 append 返回 fail（不降级）、复用 PermissionGuard write 确认。
- **原因**: 方案策划需要独立的 Story 文档记录验收标准和实现步骤。

### `plans/agent-tool-expansion/plan.md` (改)
- **修改内容**: 元数据版本 v5→v6，更新日期 2026-05-18。范围外声明更新：「集合管理 CRUD（移除 slot/重命名/迁移源手动追加），注：创建 slot 和追加条目已由 Story 24 移入范围内」。Story 清单新增 Story 24 条目（3 句话摘要 + 详细文档链接）。
- **原因**: Story 24 属于 agent-tool-expansion Epic 的追加 Story，需在总 plan 中注册。

### `plans/INDEX.md` (改)
- **修改内容**: agent-tool-expansion 行更新：Story 数 23→24，条目中追加 s24 链接。
- **原因**: 新增 Story 文档后同步更新方案索引。

### `docs/INDEX.md` (改)
- **修改内容**: agent-tool-expansion 行更新：Story 数 21→24，状态追加「+ 📝 S22已方案 + 📝 S24已方案 (1新Story)」。
- **原因**: 新增需求条目和 Story 后同步更新文档索引。
