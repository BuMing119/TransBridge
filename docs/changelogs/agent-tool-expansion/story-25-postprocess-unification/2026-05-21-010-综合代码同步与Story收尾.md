# 010: 综合代码同步与 Story 收尾（S16/S23/S25/S26 + 测试搬迁）

**日期**: 2026-05-21
**类型**: 改/删/移
**关联**: Epic: Agent 工具系统全面扩展 > S16 死代码清理 + S23 key 主索引 + S25 后处理统一 + S26 断点续传

## 修改文件

### `src/transbridge/smart_assistant/agents/agent_worker.py` (删)
- **修改内容**: 删除 AgentWorker 类（~72行），原功能为 Agent 工具执行后台线程（已由 ExecutionEngine 统一调度替代）
- **原因**: S16 死代码清理——AsyncWorker 基础设施在 agent-upgrade Phase 2 已迁移，AgentWorker 是遗留的重复实现

### `src/transbridge/smart_assistant/agents/orchestrator.py` (删)
- **修改内容**: 删除 Orchestrator 类（~120行），原功能为 Agent 编排调度（已由 StatefulDAGExecutor 替代）
- **原因**: S16 死代码清理——Graph 引擎已全面接管编排，旧 Orchestrator 不再使用

### `src/transbridge/smart_assistant/agents/__init__.py` (改)
- **修改内容**: 移除 `Orchestrator` 和 `AgentWorker` 的导入与导出
- **原因**: 对应两个已删除文件，保持包接口干净

### `src/transbridge/smart_assistant/agents/agent_registry.py` (改)
- **修改内容**: (1) 校对 Agent 工具列表新增 `translator:get_task_status` 和 `translator:stop_task`，赋予其任务控制能力；(2) 编排 Agent 工具列表新增 `proofreader:run_postprocess`，系统提示词更新 `check_quality` → `run_postprocess`
- **原因**: S25 工具统一后 Agent 能力同步——校对 Agent 需感知翻译任务状态，编排 Agent 需调用统一后处理工具

### `src/transbridge/converter/translation_entry_collection.py` (改, +158/-)
- **修改内容**: 主索引从 `id` 切换为 `key`（`_entries: key→entry`），原 `_key_index` 降级为 `_id_index` 辅助索引；`add()` 按 `key` 判定覆盖并同步维护 id 索引；新增 `get(key)` 方法；`__contains__` 和 `__iter__` 适配新主索引
- **原因**: S23 ADR-002 更新（2026-05-18）——`key` 跨 ParaTranz 同步稳定，升为主索引消除 id 不一致导致的数据分叉

### `tests/test_agent_tool_integration.py` (删)
- **修改内容**: 删除全量集成测试文件（~1071行，89测试用例，12测试类），原覆盖全链路/标签/安全/配置/ParaTranz/解析写回/Agent注册
- **原因**: 随 AgentWorker/Orchestrator 删除，旧集成测试依赖的 API 已不存在；新测试按模块拆分到 `tests/smart_assistant/` 下

### `tests/test_chat_worker.py` → `tests/smart_assistant/test_chat_worker.py` (移)
- **修改内容**: 测试文件从 tests/ 根目录搬迁到 tests/smart_assistant/ 子目录
- **原因**: smart_assistant 测试收敛到统一子目录，与项目结构对齐

### `tests/test_execution_engine.py` → `tests/smart_assistant/test_execution_engine.py` (移)
- **修改内容**: 同上，搬迁到 smart_assistant 测试子目录
- **原因**: 同上

### `tests/test_mcp.py` → `tests/smart_assistant/test_mcp.py` (移)
- **修改内容**: 同上
- **原因**: 同上

### `tests/test_memory.py` → `tests/smart_assistant/test_memory.py` (移)
- **修改内容**: 同上
- **原因**: 同上

### `tests/test_observability.py` → `tests/smart_assistant/test_observability.py` (移)
- **修改内容**: 同上
- **原因**: 同上

### `src/transbridge/smart_assistant/tools/tool_proofreader.py` (改, +340)
- **修改内容**: (1) `run_postprocess` 工具新增 `phases` 参数校验（合法值白名单+非法输入报错）、`max_workers` 参数（1-8）; (2) `entry_ids=None` 时处理全量条目而非空列表; (3) 集成 `ReportGenerator` 生成 Excel 报告; (4) 中间数据保留（refine_result/polish_result/decisions）; (5) 新增 `list_quality_reports` 工具（列出历史后处理报告）
- **原因**: S25 后处理统一——对齐 GUI PostProcessor 的完整五阶段流水线 + 报告生成能力

### `src/transbridge/smart_assistant/tools/tool_translator.py` (改, +154)
- **修改内容**: (1) `stop_task` 工具新增 `action` 参数（`"pause"`/`"resume"`/`"stop"` 三种操作）; (2) `start_translation` 结果数据补全（新增 `failed_count`/`skipped_count`）
- **原因**: S26 断点续传与暂停/恢复——`stop_task` 扩展为统一任务控制入口，支持暂停/恢复/停止三种操作

### `src/transbridge/smart_assistant/tools/task_manager.py` (改, +35)
- **修改内容**: (1) `register()` 默认创建 `pause_event`（初始 set = 非暂停状态）; (2) 新增 `pause()`/`resume()` 方法，通过 `pause_event.clear()/set()` 控制暂停; (3) `list_active()` 扩展为包含 `paused` 状态的任务; (4) `stop()` 自动唤醒暂停中的任务（set pause_event 后再标记取消）
- **原因**: S26 断点续传——TaskManager 原生支持暂停/恢复语义，`stop_task` 的 `action` 参数通过调用 `pause()`/`resume()`/`stop()` 实现

### `src/transbridge/smart_assistant/tools/tool_default.py` (改, +42)
- **修改内容**: `_register_default_tools()` 从 `ToolSpec` 构造函数改为 `ToolRegistry.register_tools()` 字典格式注册，消除注册样板代码（-35行净减），与 S16 注册样板消除一致
- **原因**: S16 注册样板消除——default namespace 工具注册方式与其他 namespace 对齐

### `src/transbridge/smart_assistant/tools/tool_paratranz.py` (改, +27)
- **修改内容**: 工具注册方式同步改为 `ToolRegistry.register_tools()` 字典格式
- **原因**: 同 tool_default.py，注册样板消除

### `src/transbridge/smart_assistant/prompts.py` (改, +23)
- **修改内容**: HYBRID_SYSTEM_PROMPT 新增「工具选择指南」节——常见场景→对应工具映射、易混淆工具对区分（set_filters vs manage_entry_labels、get_visible_entries vs get_statistics、start_translation vs set_scope）、持久化操作确认提示
- **原因**: S25/S26 工具增多后 LLM 工具选择准确率下降，通过系统提示词提供选工具指南减少误选

### `src/transbridge/ai_translator/post_processor/post_processor.py` (改, +16)
- **修改内容**: `_run_arbitration()` 性能优化——预建 quality_gate_verdict 查找表（O(N)），替代逐条调用的 `_get_quality_gate_verdict()` 方法（O(N×M)）
- **原因**: 后处理仲裁阶段在大批量条目时性能瓶颈，预建查表消除内层线性搜索

### `plans/agent-tool-expansion/plan.md` (改, +29)
- **修改内容**: 追加 Story 26（断点续传与暂停/恢复），更新 plan 状态与日期；FR9.4.7/FR9.4.8 验收标准补全
- **原因**: S26 方案确认后 plan 同步更新

### `docs/requirements.md` (改, +6)
- **修改内容**: 新增 FR9.4.7（run_postprocess 断点续传）和 FR9.4.8（run_postprocess 暂停/恢复）需求条目
- **原因**: S26 需求正式写入需求文档
