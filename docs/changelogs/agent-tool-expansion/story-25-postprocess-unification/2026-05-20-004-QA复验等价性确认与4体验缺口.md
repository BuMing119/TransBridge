# 004: QA 复验 — 后处理工具等价性确认与 4 体验缺口

**日期**: 2026-05-20
**类型**: QA 复验
**关联**: Epic: Agent 工具系统全面扩展 > Story 25: 后处理工具统一

## 修改文件

### `docs/test-reports/ai-tool-postprocess-parity-2026-05-20.md` (增)
- **修改内容**: QA 复验报告，对比 `run_postprocess` 工具 vs GUI `PostProcessor` 五阶段流水线的等价性。确认上一轮审计（003）的 11 个 Blocker/Critical 全部修复。发现 4 个体验缺口（G1: 无实时进度回调、G2: 无断点续传、G3: 完成后不通知 UI 刷新、G4: 无 API Key 前置检查）。2 个预置测试失败为测试设计缺陷非工具 bug。87/89 测试通过。
- **原因**: 用户要求检测 AI 助手工具能否达到原后处理工作流一样的效果。复验上次 QA 修复结果并给出等价性判定。

### `src/transbridge/smart_assistant/tools/tool_proofreader.py` (已验证无修改)
- **修改内容**: 验证 B1(PostProcessor 构造参数) 和 B2(register_default_checkers 调用) 已修复。当前代码通过 `PostProcessor(config)` + `processor.register_default_checkers(term_manager=term_mgr, llm_client=llm_client)` 正确初始化五阶段流水线。
- **原因**: 上一轮审计的核心 Blocker，本次复验确认修复有效。

### `src/transbridge/smart_assistant/tools/tool_translator.py` (已验证无修改)
- **修改内容**: 验证 B3(LLMPolisher 构造参数)、B4(导入路径 polisher)、B5(LLMClient 创建)、C5(_last_report 写入)、C6(intensity 值映射) 均已修复。当前代码通过 `create_llm_client()` + `_level_map` + `LLMPolisher(llm_client=llm_client, polish_level=polish_level)` 正确运行。
- **原因**: 上一轮审计的 5 项问题全部修复，本次复验确认。

### `src/transbridge/smart_assistant/tools/tool_parser.py` (已验证无修改)
- **修改内容**: 验证 N2(sst_parser 导入路径 `parser.xt.sst_parser`) 已修复。N3(import_strings) 已改为友好报错信息。
- **原因**: 确认导入路径修复有效。

### `src/transbridge/smart_assistant/tools/tool_paratranz.py` (已验证无修改)
- **修改内容**: 验证 N1(ParatranzClient 导入路径 `from src.transbridge.paratranz import ParatranzClient`) 已修复。
- **原因**: 确认 PT 工具不再因 ModuleNotFoundError 崩溃。

### `src/transbridge/smart_assistant/agents/agent_registry.py` (已验证无修改)
- **修改内容**: 验证 N4(orchestrator namespace `default:get_statistics`) 已修复。`_expand_wildcard` 已重构为 `@staticmethod`。
- **原因**: 确认 Agent 注册 namespace 引用正确。
