# 001: Story 19 write_back 合并 4→1

**日期**: 2026-05-18
**类型**: 改
**关联**: Epic: Agent 工具系统全面扩展 > Story 19: write_back 合并 (4→1)

## 修改文件

### `src/transbridge/smart_assistant/tools/tool_writer.py` (全文重写)
- **修改内容**:
  1. 新增 `import warnings`，文档字符串添加 Story 19 说明
  2. 4 个原写回函数重命名为 `_write_to_*_impl`（去除 `@require_collection`，参数改为直接传入 slot/collection/path）
  3. 新增 `_WRITE_HANDLERS` dispatch 表：`{"esp": _write_to_esp_impl, "eet": _write_to_eet_impl, "xt": _write_to_xt_impl, "strings": _write_to_strings_impl}`
  4. 新增 `@require_collection` 装饰的 `_tool_write_back()` 统一入口——解析 `target` 参数，查 dispatch 表路由，处理默认路径推断（esp→ctx.esp_path, eet→ctx.eet_path, xt→ctx.xt_path, strings→output_dir）
  5. 4 个旧工具函数保留 deprecated wrapper：`_tool_write_to_esp/eet/xt/strings`，发出 `DeprecationWarning` 后转发到 `_tool_write_back({"target": "...", ...}, ctx)`
  6. `_PARAM_SCHEMAS` 新增 `"write_back"`（target 必填 + path 可选），旧 4 个 schema 保留
  7. `_register_writer_tools()` 注册表：4 旧条目替换为 1 个 `write_back`（admin, require_confirmation=True, is_long_running=True）
- **原因**: Story 19（工具架构合并路线A）——4 个写回工具按目标文件格式分裂，合并到统一入口 `write_back` 通过 dispatch 表路由，消除碎片并简化 LLM 工具选择

## 测试结果

- 86 passed, 3 failed（全部为预存问题）
- Writer namespace: 4→1 工具（-3），总计非废弃: 51→48（-3）
