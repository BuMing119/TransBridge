# 004: Story 文档批量 v2 更新

**日期**: 2026-05-11
**类型**: 改
**关联**: Epic: Agent 工具系统全面扩展 > Story 细化阶段

## 修改文件

### `plans/agent-tool-expansion/stories/story-01-infra-tools-package.md` (改)
- **修改内容**: 全面重写为 v2 版本。标题改为「基础设施搭建 + 核心基础设施」。新增 10 项验收标准（B2字典兼容/B3语义修正/B4ExecutionContext/H5HITL协议/B6GuardChain/H8公共筛选函数/H9代理/E1路径遍历/E5装饰器规范/E12输出脱敏递归）。实现步骤从 6 步扩展为 11 步。新增 ExecutionContext/HITLRequest/HITLResponse/execute_with_guardrails/_filter_entries 关键接口定义。涉及文件从 5 个增至 7 个（+input_validator/+output_validator）
- **原因**: Story 01 从基础搭建升级为工具系统核心基础设施，确保后续所有 Story 有统一的技术底座

### `plans/agent-tool-expansion/stories/story-02-task-manager.md` (改)
- **修改内容**: TaskHandle 新增 pause_event 预留字段（默认None，B5联动）和 _thread 引用（O9）。TaskManager 单例改为模块级双重检查锁（E3）。get_status() progress 返回深拷贝（E3）。cleanup() 增加线程 join(timeout=5)（O9）。数据流移除 pause_task 分支
- **原因**: 线程安全强化，防止并发竞态和内存泄漏

### `plans/agent-tool-expansion/stories/story-03-appcontext-viewmodel.md` (改)
- **修改内容**: 验收标准新增 B1（label_library/entry_labels/label_data_changed 上移）、E8（translation_scope 正式属性含类型校验）、E6（search_field↔Step2搜索框映射契约）。涉及文件从 1 个增至 3 个（+step2.py +映射契约文档）。注意事项新增标签数据持久化链路说明和映射契约详表
- **原因**: 标签数据从 UI 层上移至 AppContext，消除跨组基础设施缺口

### `plans/agent-tool-expansion/stories/story-04-p0-filter-search-tools.md` (改)
- **修改内容**: 标题改为「筛选+搜索+编辑+选择+批量标记」。验收标准从 7 项扩展为 10 项（+select_entries 用 _selected_ids 独立集合(H2) +edit_translation 增加可选 new_stage(H4) +set_stage 批量标记(H3) +_filter_entries 复用(H8)）。工具从 6 个增至 9 个
- **原因**: 合并原 Story 05，消除跨 Story 依赖，形成筛选→选择→编辑→标记完整操作链

### `plans/agent-tool-expansion/stories/story-05-p0-edit-select-tools.md` (改)
- **修改内容**: 状态从「已确认」改为「⛔ 废弃」。新增废弃日期、废弃原因说明。保留原文档内容仅供历史参考
- **原因**: 内容已合并至 Story 04，减少跨 Story 依赖

### `plans/agent-tool-expansion/stories/story-06-p0-translation-control.md` (改)
- **修改内容**: 头部更新 v2 状态标注（-pause_task(B5) +stop_all_tasks(E7)）
- **原因**: 移除假暂停工具，语义显式化

### `plans/agent-tool-expansion/stories/story-07-p0-state-query-proofread.md` (改)
- **修改内容**: 头部更新 v2 状态标注（get_collection_summary deprecated→合并到 get_statistics(O8)）
- **原因**: 消除功能重复，统一统计入口

### `plans/agent-tool-expansion/stories/story-08-p1-label-tools.md` (改)
- **修改内容**: 头部更新 v2 状态标注（+前置依赖 Story 03 标签数据(B1联动) +复用 _filter_entries(H8)）
- **原因**: 声明硬依赖，确保 Story 03 先于 08 完成

### `plans/agent-tool-expansion/stories/story-09-p1-translation-config.md` (改)
- **修改内容**: 验收标准全面更新：set_translation_config 移除 base_url 参数，新增 profile 参数（H7用户方案：INI [llm_profiles] 预设端点方案）。set_scope 操作 ctx.translation_scope 正式属性（E8类型校验）。新增 INI [llm_profiles] 配置节验收项
- **原因**: 安全改造——堵住 Agent 自由输入 URL 的攻击面，同时保留切换端点灵活性

### `plans/agent-tool-expansion/stories/story-10-p1-postprocess-tools.md` (改)
- **修改内容**: 头部更新 v2 状态标注（+LLM后处理 require_confirmation(E10) +PostProcessor 工厂函数(E9)）
- **原因**: API 费用防护 + 减少胶水代码重复

### `plans/agent-tool-expansion/stories/story-11-p1-paratranz-tools.md` (改)
- **修改内容**: 头部更新 v2 状态标注（download_entries 单阶段重构(O7) +API surface 确认(O10)）
- **原因**: 消除两阶段交互的语义裂痕，实施前确认 API 覆盖度

### `plans/agent-tool-expansion/stories/story-12-p2-parser-writer-project.md` (改)
- **修改内容**: 头部更新 v2 状态标注（parser 6工具 write→read(H6) +文件扩展名白名单(E1) +路径遍历强化(E1)）
- **原因**: 权限分级精准化，解析不产生持久化副作用，应为 read

### `plans/agent-tool-expansion/stories/story-13-agent-integration.md` (改)
- **修改内容**: 头部更新 v2 状态标注（-ExecutionContext 定义移至 Story 01 +MCP 护栏接入(B6联动) +map_to_steps 修复(E4) +namespace 通配符(O3)）
- **原因**: 范围缩减（ExecutionContext 前置），MCP 安全分叉修复

### `plans/agent-tool-expansion/stories/story-14-integration-tests.md` (新)
- **修改内容**: 新建 Story 14 文档。7 个验收标准组（完整链路/标签系统/安全护栏/翻译配置/ParaTranz/文件解析/文件写回），7 个实现步骤，1 个测试文件
- **原因**: 跨 Story 集成测试覆盖完整工作流，补全测试策略空白

### `plans/INDEX.md` (改)
- **修改内容**: agent-tool-expansion 条目的 Story 05 标注 ⛔ 废弃，新增 s14 链接
- **原因**: 与 Story 文档状态同步
