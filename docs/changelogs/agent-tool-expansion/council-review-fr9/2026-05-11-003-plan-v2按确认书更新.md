# 003: plan.md v2 按确认书更新

**日期**: 2026-05-11
**类型**: 改
**关联**: Epic: Agent 工具系统全面扩展 > 方案策划阶段

## 修改文件

### `plans/agent-tool-expansion/plan.md` (改)
- **修改内容**: 全面重写为 v2 版本，按 `modification-confirmation.md` 逐项落实 35 项确认修改：
  - **Story 01** 从基础搭建升级为核心基础设施：+ExecutionContext 数据类（含 __getattr__ 代理）+HITLRequest/HITLResponse 协议 +execute_with_guardrails() 统一入口 +_filter_entries() 公共函数 +基础路径遍历检测 +输出脱敏 list 递归 +装饰器顺序规范文档 +ToolResult.success 从三态改为 bool+partial +ToolResult.get()/__getitem__ 字典兼容
  - **Story 02** 扩大：+get_status() progress 深拷贝 +单例双重检查锁 +TaskHandle.pause_event 预留 +线程引用跟踪（daemon=True, cleanup join）
  - **Story 03** 扩大：+label_library/entry_labels 从 UI 层上移 +label_data_changed 信号 +_translation_scope 正式属性（类型校验）+filter_state 映射契约文档
  - **Story 04** 合并原 Story 05：+set_stage 批量标记工具 +_selected_ids 独立集合（解耦标签系统）+edit_translation 增加可选 new_stage 参数
  - **Story 05** 标记 ⛔ 废弃，内容并入 Story 04
  - **Story 06** 缩减：移除 pause_task（假暂停）+stop_task 必传 task_id +新增 stop_all_tasks 独立工具
  - **Story 07** 微调：get_collection_summary 标记 deprecated，功能合并到 get_statistics
  - **Story 08** 新增前置依赖声明（硬依赖 Story 03 标签数据上移）+复用 _filter_entries
  - **Story 09** 安全改造：移除 base_url 自由输入 → profile 预设方案切换（INI [llm_profiles] 配置节），Agent 只能切换预设方案不能自由输入 URL
  - **Story 10** 安全增强：3 个 LLM 后处理工具 require_confirmation=true（显示预估费用）+提取 _run_postprocess_phase() 工厂函数
  - **Story 11** 设计调整：download_entries 单阶段重构（下载+对比摘要合一）+实施前 API surface 审查
  - **Story 12** 权限修正：parser 6 工具 write→read +文件扩展名白名单（.esp/.esm/.esl/.xml/.json/.strings）+路径遍历强化
  - **Story 13** 范围缩减：移除 ExecutionContext 定义（已移至 Story 01）+MCP 护栏接入（adapter.py 改用 execute_with_guardrails）+map_to_steps() 修复 +namespace:* 通配符
  - **新增 Story 14**: 跨 Story 集成测试（筛选→选择→翻译→标记完整链路 + 安全护栏 + profile 切换 + ParaTranz + parser/writer）
  - **新增章节**: 独立 PR（ParaTranz 限流/审计日志/ToolSpec 迁移）+P2 后续迭代（真实暂停/恢复 + Reflexion 写工具条件重试）
- **原因**: 基于 9 人评审委员会的 38 条建议和用户逐项确认结果，将原 plan.md 从评审前版本升级为评审后确认版本，作为后续 Story 细化文档和编码实现的唯一依据

### `plans/INDEX.md` (改)
- **修改内容**: agent-tool-expansion 状态从「📋 修改已确认（待按确认书更新 plan.md）」→「✅ 已确认 (v2)」；Story 05 标注 ⛔ 废弃；新增 s14 链接
- **原因**: 与 plan.md v2 状态同步

### `docs/INDEX.md` (改)
- **修改内容**: agent-tool-expansion 方案表状态从「📋 修改已确认（35/38 项，待按确认书更新 plan.md）」→「✅ 已确认 (v2 — 按评审建议更新)」
- **原因**: 与 plan.md v2 状态同步
