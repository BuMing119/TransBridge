# 002: 单元测试补全 — fileops + migrator

**日期**: 2026-08-14
**类型**: 增
**关联**: Epic: 通用文件与词条工具 > Story 01-04 测试

## 修改文件

### tests/test_fileops.py (增)
- **修改内容**: 7 个用例——zip 打包/解包往返、分层提取、不支持格式报错、目录 diff（增删改不变）、skip_hash 大文件、资源过滤默认规则、目录级规则覆盖。fixture 用工作区临时目录（DSH 沙箱仅允许写工作区，不能用 pytest tmp_path/系统 Temp）
- **原因**: 覆盖 fileops 三模块（archive/differ/filter_rules）

### tests/test_key_migrator.py (增)
- **修改内容**: 5 个用例——键命中继承、原文变化需复核、键未命中、空旧集合、换行空白规范化
- **原因**: 覆盖 migrator.key_migrator 键对齐逻辑