# 001: Story05 编码 — 词典套用/存词典 Agent 工具 + id/key 语义修正

**日期**: 2026-08-14
**类型**: 改
**关联**: Epic: 通用文件与词条工具 > Story 05: 词典套用与存词典 Agent 工具注册

## 修改文件

### src/transbridge/smart_assistant/tools/tool_migrator.py (增)
- **修改内容**: 注册 apply_dictionary / save_dictionary 到 translator namespace（permission=write）。apply_dictionary 调 TranslationMemoryManager.apply_to_collection()，save_dictionary 调 save_from_collection()
- **原因**: FR16.5，补齐词典套用/存词典的 Agent 工具缺口（translation_memory 已有能力，仅注册）

### src/transbridge/translation_memory/manager.py (改)
- **修改内容**: save_from_collection/apply_to_collection 内部的 5 处 e.id 统一改为 e.key（self.add(e.key,...)、query(e.key,...)、needs_review.append(e.key)、c[entry_id]=e.key 等），消除与 Collection 主索引（entry.key）的分叉
- **原因**: ADR-015 更新节核查结论——entry.key 是唯一主索引，translation_memory 用 e.id 属历史遗留隐患（当前 id==key 故不报错，但应统一）