# Story 01：可复现 QA Evidence Manifest

- 日期：2026-08-18
- Epic/Story：`release-hardening-v2/S01`
- 追溯：共同验收规则、NFR6.1、ADR-016、R-047/R-048
- 状态：实现完成，增量验证通过；待综合 QA

## 实现增量

- 新增版本化 `EvidenceManifest`，记录 Git HEAD/dirty/diff、OS/硬件/Python、显式环境白名单、pyproject/lock hash、命令真实退出码、stdout/stderr 与 artifact snapshot/hash。
- verdict 从真实退出码和证据错误重新派生；失败记录不能通过手工改摘要变为 passed，manifest 原子写入且拒绝覆盖。
- 新增路径边界、artifact 损坏、secret 参数阻断与脱敏、缺 lock/Git/项目 Python blocker；不静默切换到系统 Python。
- 提供 `uv sync --frozen` 的 Python 3.12 bootstrap、evidence run/verify/replay CLI；重放先验证原证据并比较 artifact hash。

## 验证证据

- `uv run --locked pytest tests/quality/test_evidence.py ...`：8 passed。
- 全量回归：644 passed；Ruff/format/compileall 通过。
- [EvidenceManifest](../../../test-reports/requirement-code-review-2026-08-18/qa-evidence/release-s01/qa-20260818T062045.127691Z-3ccc8a8af032/manifest.json)：验证结果 `passed`，随后由独立 verify CLI 复验 schema/verdict/hash 有效。
- uv 受管 Python 3.12.12 实测可用；先前子 Agent 的失败是受限上下文误判，未重建有效 `.venv`。
- 未执行 Git commit/push。

## 剩余门禁

该 Story 提供证据机制，不把当前开发机测试冒充最终 Windows clean build/installer QA。后续各 Story 需实际生成 manifest；release S05 汇总最终发行证据。
