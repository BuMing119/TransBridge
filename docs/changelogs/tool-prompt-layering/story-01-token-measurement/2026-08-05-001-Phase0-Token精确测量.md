# 001: Phase 0 Token 精确测量完成

**日期**: 2026-08-05
**类型**: 增
**关联**: Epic: 工具提示词分层加载 > Story 01: Phase 0 — Token 精确测量

## 修改文件

### `scripts/measure_tokens.py` (增)
- **修改内容**: 新建 Token 测量脚本 (~180 行)。使用 tiktoken `cl100k_base` 编码（DeepSeek-v4 近似）测量 `build_system_prompt()` 输出的各段 token 数。覆盖 7 项测量维度：模板正文、上下文、预加载工具、get_tool_help Schema、路由表、工具目录、42 个工具各自 Schema token 排名。同时对比分层模式 vs 全量 Schema 模式的 token 节省率。
- **原因**: Story 01 (Phase 0) 要求产出精确的 system prompt token 基线数据，为 Phase 4 调优提供量化依据。脚本独立于项目源码，避免污染核心代码库。

### `docs/temp/tool-prompt-layering-token-measurement.md` (增)
- **修改内容**: 测量报告文档，包含 8 个章节——各段 Token 测量、工具段细分、工具 Schema Token 排名 (Top 20)、统计汇总、分层 vs 全量对比、各 Namespace 分布、Phase 4 Baseline、发现的问题。
- **原因**: Story 01 验收标准要求产出结构化测量报告。

## 关键测量结果

| 指标 | 值 |
|------|-----|
| 分层 system prompt (with context) | **3,365 tokens** |
| 全量 Schema system prompt | **9,183 tokens** |
| Token 节省 | **5,818 (63.4%)** |
| 工具目录 | 1,324 tokens (vs 预估 ~500) |
| 活跃工具 | 42 个 / 7 namespace |
| 最大单工具 Schema | set_filters: 407 tokens |

## 额外发现

- **工具目录超预估**: 实际 1,324 tokens vs 方案预估 ~500 tokens，42 个工具 × ~31 tokens/行 累积超出预期，S05 可考虑缩短 summary 或优化目录格式
- **双重导入隐患**: `tools/` 子包内模块使用 `from src.transbridge...` 导入，与 `prompts.py`/`tool_registry.py` 的相对导入创建两个独立的 `_ToolRegistry` 类实例，工具注册到其中一个但查询另一个为空。建议 S05 统一导入路径
