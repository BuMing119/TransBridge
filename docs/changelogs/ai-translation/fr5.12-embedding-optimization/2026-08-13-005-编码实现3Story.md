# 005: 编码实现 FR5.12 三项增强

**日期**: 2026-08-13
**类型**: 改
**关联**: Epic: AI 翻译 > FR5.12 embedding 语义检索优化（编码实现）

## 修改文件

### `src/transbridge/ai_translator/term_vector_index.py` (改)
- **修改内容**: 增量构建（`_row_map`/`_inactive_rows` 软删除 + 失效超 20% 压缩）+ LRU 编码缓存（默认 512）+ BM25 融合检索（`search_hybrid`/`search_hybrid_batch`，向量与 BM25 加权求和，权重可配）
- **原因**: 实现 FR5.12.3（增量索引）/ FR5.12.4（编码缓存）/ FR5.12.5（BM25 混合检索）

### `src/transbridge/ai_translator/term_database.py` (改)
- **修改内容**: `match_terms_enhanced` 语义召回改 `search_batch` 批量 + 放开 `[:10]` 上限 + 阈值统一 0.7 + 接入 `search_hybrid_batch` + `_init_vector_index` 传 `bm25_weight`
- **原因**: 实现 FR5.12.1（批量召回）/ FR5.12.2（阈值统一）

### `src/transbridge/config/llm.py` (改)
- **修改内容**: 新增 `bm25_weight` 配置字段（默认 0.5）+ `_CONFIG_FIELDS` 追加持久化项
- **原因**: BM25 融合权重可配（ADR-013）

### `pyproject.toml` (改)
- **修改内容**: 新增 `rank-bm25>=0.2.2` 依赖
- **原因**: BM25 混合检索依赖（ADR-013 选型）

### `tests/ai_translator/test_term_vector_index.py` (增)
- **修改内容**: 9 个测试覆盖增量构建/软删除过滤/编码缓存/BM25 融合/阈值一致性
- **原因**: FR5.12 回归保护
