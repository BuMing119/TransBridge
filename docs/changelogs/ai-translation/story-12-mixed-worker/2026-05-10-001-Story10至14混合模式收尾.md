# 001: Story-10~14 混合模式功能收尾

**日期**: 2026-05-10
**类型**: 增/改
**关联**: Epic: ai-translation > Story 10~14: FR5.11 混合模式

## 修改文件

### `src/transbridge/paratranz/config_manager.py` (改)
- **修改内容**: 新增 `apply_rules(rules, entries) -> dict` 函数。按规则优先级为条目分配动作（translate/polish/skip），未匹配默认 skip。修复 `ai_translator_window.py` 中 `from ... import apply_rules` 的 ImportError
- **原因**: 规则匹配引擎在 config 提取时遗漏，ai_translator_window 导入报错

### `src/transbridge/ui/tools/ai_translator/_mixed_worker.py` (增)
- **修改内容**: 新建 `_MixedWorker(QThread)` + `MixedProgress` 数据类。串行模式（先翻译后润色）+ 并行模式（Thread 并发）。复用现有 AutoTranslator + LLMPolisher。信号：progress/finished/error，支持 cancel()
- **原因**: Story-12 核心组件，统一调度混合执行流程

### `src/transbridge/ui/tools/ai_translator/ai_translator_window.py` (改)
- **修改内容**: `_on_mixed_start()` 替换占位 QMessageBox 为真实 _MixedWorker 调用。新增 `_on_mixed_finished()` 回调（汇总翻译/润色结果并弹窗）。连接 finished/error 信号
- **原因**: Story-11/14 集成收尾，混合模式从占位变为可运行

## 已有实现（本次验证确认）
- `_rule_editor_widget.py` — 规则编辑器 QTableWidget（Story-10，前期已完成）
- `ai_translator_window.py` 混合模式 UI — RadioButton + 面板 + 规则编辑器 + 执行顺序下拉（Story-11，前期已完成）
- `LLMConfig.action_rules` 持久化 — save_to_file/load_from_file 支持（Story-10，前期已完成）
- `ActionRule` 模型 — config_manager.py 中定义（Story-10，前期已完成）

## ai-translation Epic 最终状态
14/14 Story 全部完成（01~09 前期 + 10~14 本次收尾）
