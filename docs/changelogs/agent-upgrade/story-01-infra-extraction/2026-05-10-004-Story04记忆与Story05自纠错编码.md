# 004: Story-04 长期记忆 + Story-05 Reflexion 编码

**关联**: agent-upgrade > Story 04 & Story 05

## Story-04 修改文件
### `memory/__init__.py`(增) — 子包导出
### `memory/memory_store.py`(增) — MemoryEntry+MemoryStore（FAISS+JSON双存储，三模式：api/local/disabled）
### `memory/memory_retriever.py`(增) — 两阶段召回（精确→语义）

## Story-05 修改文件
### `reflexion/__init__.py`(增) — 子包导出
### `reflexion/retry_handler.py`(增) — RetryHandler（LLM分析→调整→重试max3次）
### `execution_engine.py`(改) — _run_single注入重试循环+step_retrying信号+_retry_handler注入点
