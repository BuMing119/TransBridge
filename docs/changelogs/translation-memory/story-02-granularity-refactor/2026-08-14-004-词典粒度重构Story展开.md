# 004: 词典粒度重构 Story 展开（S06-10）

**日期**: 2026-08-14
**类型**: 增
**关联**: Epic: 翻译记忆（词典）系统 > Story 02: 词典粒度重构（一文件一 mod + 多词典组合查询）

## 修改文件

### `plans/translation-memory/stories/story-06-model-refactor.md` (增)
- **修改内容**: 展开 Story 06 数据模型重构：`DictionaryEntry` 字段重构（`source`→`source_mod` + 新增 `form_id_with_plugin`）、`Dictionary` 字段重构（`scope_id`→`mod_file_id` + 单值 scope）、词条主键 `sha1(mod名|原文)`、`.tbdict` 后缀、scope 校验。
- **原因**: 将 plan 验收标准展开为可编码的数据流/接口/边界/伪代码/测试策略。

### `plans/translation-memory/stories/story-07-locate-load-refactor.md` (增)
- **修改内容**: 展开 Story 07 定位/命名/加载重构：定位键 `(scope,scope_id)`→`mod_file_id`、文件名 `{mod}.tbdict`、load 硬校验同名、旧 `*.json` 弃置、`save_from_collection` 推断 source_mod + 持久化 form_id_with_plugin。
- **原因**: 同上。

### `plans/translation-memory/stories/story-08-multi-dict-query.md` (增)
- **修改内容**: 展开 Story 08 多词典组合查询与冲突仲裁（性能重点）：激活集惰性构建、同名 mod 命中即短路、跨词典候选收集 + `_arbitrate` 仲裁、`conflicts` 真正填充、`_normalize_cache` 加锁与分级缓存。明确性能约束（避免线性扫全词典）。
- **原因**: 用户要求「Story 08 重点策划性能开销」，故单列性能策略与回退方案。

### `plans/translation-memory/stories/story-09-scope-share.md` (增)
- **修改内容**: 展开 Story 09 scope 修改 + 分享/导入：`set_scope`/`import_dict`/`export_dict` 接口 + GUI 三按钮（导入/导出/打开目录）+ 同名覆盖/跳过交互。
- **原因**: 同上。

### `plans/translation-memory/stories/story-10-gui-arbitration.md` (增)
- **修改内容**: 展开 Story 10 GUI 面板改造 + 冲突仲裁对话框：mod 粒度列表、激活集默认自动、`DictionaryConflictDialog` 冲突可视化仲裁、存词典 mod 名推断。
- **原因**: 同上。

### `plans/translation-memory/plan.md` (改)
- **修改内容**: 5 个 Story 的「详细实现指南」占位链接替换为 `**详细文档**` 实际路径。
- **原因**: bm-story 步骤 4 的 plan.md 索引化清理。
