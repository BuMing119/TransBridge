# 002: 后端包创建与 6 文件 git mv 搬迁

**日期**: 2026-05-10
**类型**: 移
**关联**: Epic: llm-chat > Story 06: 新建后端包 + 文件搬迁

## 修改文件

### `src/transbridge/smart_assistant/__init__.py` (增)
- **修改内容**: 新建包入口文件，导出 8 个公开符号：ConversationManager / ChatWorker / ExecutionEngine / StepResult / ToolRegistry / ToolSpec / ContextBuilder / build_system_prompt。使用包内相对导入引用同包模块
- **原因**: smart_assistant 后端包需要公开 API 供 UI 层和外部调用，遵循 ADR-008 分层规范

### `src/transbridge/smart_assistant/conversation_manager.py` (移)
- **修改内容**: 从 `ui/tools/smart_assistant/` 搬迁至 `smart_assistant/`，文件内容不变
- **原因**: 对话管理器属于业务逻辑层，不应放在 UI 目录下

### `src/transbridge/smart_assistant/chat_worker.py` (移)
- **修改内容**: 从 `ui/tools/smart_assistant/` 搬迁至 `smart_assistant/`，文件内容不变
- **原因**: LLM 调用线程属于业务逻辑层

### `src/transbridge/smart_assistant/execution_engine.py` (移)
- **修改内容**: 从 `ui/tools/smart_assistant/` 搬迁至 `smart_assistant/`，文件内容不变。含 StepResult 数据类和 ExecutionEngine 执行引擎
- **原因**: DAG 执行引擎属于业务逻辑层

### `src/transbridge/smart_assistant/tool_registry.py` (移)
- **修改内容**: 从 `ui/tools/smart_assistant/` 搬迁至 `smart_assistant/`，文件内容不变。含 ToolSpec/ToolRegistry 和 6 个 v1 工具实现函数
- **原因**: 工具注册表和工具实现属于业务逻辑层

### `src/transbridge/smart_assistant/context_builder.py` (移)
- **修改内容**: 从 `ui/tools/smart_assistant/` 搬迁至 `smart_assistant/`，文件内容不变。含 ContextBuilder.build() 方法
- **原因**: 上下文构建器属于业务逻辑层

### `src/transbridge/smart_assistant/prompts.py` (移)
- **修改内容**: 从 `ui/tools/smart_assistant/` 搬迁至 `smart_assistant/`，文件内容不变。含 HYBRID_SYSTEM_PROMPT 模板和 build_system_prompt() 函数。包内相对导入 `from .tool_registry import ToolRegistry` 搬迁后仍有效
- **原因**: System Prompt 模板属于业务逻辑层
