# 001: 循环控制与 UI 卡片

**日期**: 2026-05-06
**类型**: 增
**关联**: Epic: 智能助手侧边栏面板 > Story 03: 循环控制与 UI 卡片

## 修改文件

### `src/transbridge/ui/tools/smart_assistant/tool_card.py` (增)
- **修改内容**: 新建 ToolCard（黄色背景，显示工具名+参数表+执行/忽略按钮，set_result 显示完成状态）和 BatchToolCard（多步骤概览+全部执行按钮）。executed/ignored/all_executed 信号
- **原因**: ReAct 模式下用户确认工具调用的 UI 组件

### `src/transbridge/ui/tools/smart_assistant/plan_card.py` (增)
- **修改内容**: 新建 PlanCard（蓝色背景，QListWidget 步骤列表+依赖标注+进度文字+执行计划/取消按钮）。on_step_started/on_step_finished/on_progress/on_all_finished 方法供 ExecutionEngine 信号驱动更新。confirmed/cancelled 信号
- **原因**: 计划模式下用户确认执行计划的 UI 组件

### `src/transbridge/ui/tools/smart_assistant/tool_registry.py` (增)
- **修改内容**: 新建 ToolSpec dataclass + _ToolRegistry 类级别单例（register/get/build_tool_schema_for_prompt）。Story-03 桩实现，完整工具注册留给 Story-04
- **原因**: ExecutionEngine 依赖 ToolRegistry 接口，先提供最小桩代码保证编译通过

### `src/transbridge/ui/tools/smart_assistant/chat_widget.py` (改)
- **修改内容**: 从简单消息组件重写为完整循环控制中枢。新增：ConversationManager 多轮对话管理、ChatWorker LLM 调用、PromptBuilder 响应解析、ExecutionEngine 计划执行。_run_llm_round() → _on_llm_finished() → parse_hybrid_response → mode 分发（PlanCard/ToolCard/BatchToolCard/纯文本）。ReAct 深度限制(_MAX_REACT_DEPTH=10)。_on_send 改为走 ConversationManager + LLM 流程。预留 ToolRegistry 占位执行。原 message_sent 信号保留
- **原因**: 实现 ReAct + Plan 双模式混合循环控制的核心逻辑，连接 Story-01 UI 层和 Story-02 后端层
