# 仓库全局 Agent 规范

- **日期**: 2026-08-24
- **Epic**: `maintenance/repository-agent-governance`
- **Story**: 非计划仓库治理维护
- **Git 证据**: `25f3888eccc83d805ade04b03f7660618ddd9033` 中的 `AGENTS.md`

## 变更目的

为仓库内的编码代理提供一份全局适用、可直接执行的项目规范，统一环境、代码边界、规模治理、仓库清洁和交接要求。

## 文件级变化

- `AGENTS.md`（新增）：说明 TransBridge 的项目范围、源码与测试目录、CLI/MCP 入口，以及领域逻辑与 PyQt UI 的边界。
- `AGENTS.md`（新增）：明确使用仓库现有 `uv` 环境和锁文件，并给出聚焦测试、Ruff 检查及 Pytest marker 约定。
- `AGENTS.md`（新增）：统一格式、兼容性、回归测试、UI 响应性和异常处理要求；修改代码、配置或文档时必须作用于权威来源和根因，不在正文中遗留补丁层痕迹。
- `AGENTS.md`（新增）：为手写生产文件和主类设置责任审查阈值；超过 500 行或 30 个方法需要审查，超过 700 行或 40 个方法通常必须拆分，且禁止用通用 helper、misc、mixin、深继承或新的上帝类隐藏规模。
- `AGENTS.md`（新增）：要求保留无关工作区改动、谨慎处理锁文件和构建配置，并在任务结束时核实归属后清理本次创建的临时文件。
- `AGENTS.md`（新增）：要求交接时说明实际验证命令、未运行检查、用户可见变化及兼容或迁移影响。

## 验证证据

- `git show --format='' 25f3888 -- AGENTS.md`：确认该提交新增 47 行仓库级规范。
- 复读新记录并验证索引链接、编号唯一性和 Markdown 行尾空格；检查通过。
- 本次仅记录规范文档，不涉及可执行代码，因此未新增专项测试。

## 遗留项

无。
