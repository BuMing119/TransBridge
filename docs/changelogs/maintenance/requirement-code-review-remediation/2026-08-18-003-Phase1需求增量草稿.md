# Phase 1 综合整改需求增量草稿

- 日期：2026-08-18
- 归属：`maintenance/requirement-code-review-remediation`
- 阶段：bm-pilot Phase 1
- 状态：需求待用户确认

## 变更目的

将 Phase 0 已确认的复杂整改路线和 50 项去重根因转化为可验证的需求合同。只描述目标行为、边界、失败语义和验收门禁，不提前确定内部模块实现，也不把历史“代码已存在”继续视为当前已验收。

## 文件增量

### 更新 `docs/requirements.md`

- 保留 FR1～FR16 历史正文，新增“综合整改需求增量（草稿，2026-08-18）”。
- 新增 FR17 多入口业务合同一致性、FR18 翻译 I/O/条目身份/发布、FR19 Project/Variant/Session 生命周期、FR20 统一长任务、FR21 翻译与后处理工作流、FR22 ParaTranz 文件与网络同步、FR23 FOMOD 与归档发布合同。
- 新增可测性能、可靠性、兼容性、安全、扩展与 Windows 分发 NFR。
- 固化 ParaTranz JSON `key`/`id` 双 ID、入口等价、互斥终态、替换式 Variant 物化、checkpoint 幂等、原子发布、凭据脱敏和归档安全等验收行为。
- 保留三项待确认决策：locked 空译文策略、历史格式支持级别、初始性能/稳定性预算。

### 更新 `docs/INDEX.md`

- 将需求文档状态从笼统“已实现”纠正为“历史实现部分核验；整改草稿待确认”。
- 说明新增 FR17～FR23 与 NFR 增量覆盖范围，不改动其他文档索引状态。

### 更新整改台账和 changelog 索引

- 在 `remediation-ledger.md` 增加 R-001～R-050 到新需求的追溯关系和 Phase 1 出口门禁。
- 将当前状态推进到“等待需求确认”，并链接本记录。

## 验证

1. `git diff --check` 通过，仅有仓库既有换行格式提示。
2. 使用 `rg` 核对 FR17～FR23、三项确认事项、变更历史和需求索引入口均存在。
3. 复核需求增量未修改 ADR、Plan、Story、业务代码或正式审查报告，三个 `.partial.md` 保持只读。
4. 本阶段为文档合同增量，未运行代码测试；后续测试须按 Story 增量执行，不能以本记录证明功能完成。

## 遗留与门禁

- 用户确认三项开放决策及整体需求前，不得进入 Phase 2 架构增量。
- 需求确认后需把草稿状态更新为“已确认”，追加独立门禁记录，再调用 bm-arch 评估 ADR-001～ADR-015 和必要的新 ADR。
- 未执行 Git commit 或 push。
