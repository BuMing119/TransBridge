# Phase 2 综合整改架构确认

- 日期：2026-08-18
- 归属：`maintenance/requirement-code-review-remediation`
- 阶段：bm-pilot Phase 2
- 状态：架构已确认

## 确认结果

用户以“继续”确认 Phase 2 架构路线。ADR-016～019 和 ADR-001、002、004、006、008～015 的 2026-08-18 增量由“提议”提升为“已接受”。ADR-003、005、007 保留历史决策，其执行受新 ADR 的应用边界、I/O、状态所有权和任务运行时合同约束。

## 接受的核心边界

- 采用模块化单体、显式 Application Layer、Ports/Adapters 与单一 Composition Root。
- Parser/Writer 统一到 FormatAdapter 合同，内部 EntryKey 与外部引用分离，发布使用 staging、验证和原子替换。
- Project/Variant/Session 使用明确权威所有者、Repository/UnitOfWork、完整替换式物化和 schema migration/quarantine。
- 长任务统一由 TaskRuntime 管理 owner、run_id、状态机、取消提交屏障、checkpoint 与订阅生命周期。
- GUI、Agent、MCP、FOMOD 通过相同 use case 合同进入业务；兼容入口仅委托，不形成第二套状态或规则。

## 文件变更

- 将 `docs/adr/016-*.md`～`019-*.md` 状态提升为已接受。
- 将 12 份既有 ADR 的 2026-08-18 增量标记为已接受，未改写其历史背景。
- 同步 `docs/INDEX.md`、整改台账和 changelog 索引。

## 记录门禁

1. 架构接受状态与索引一致。
2. 接受状态不等同于代码实现或需求验收。
3. 本阶段未修改 Plan、Story、业务代码或正式审查报告，未执行 Git commit/push。
4. 下一阶段使用 `bm-plan` 组织少量 V2 Plan；Plan 未确认前不展开 Story 或编码。
