# Phase 0 问题台账与路线门禁

- 日期：2026-08-18
- 归属：`maintenance/requirement-code-review-remediation`
- 阶段：bm-pilot Phase 0
- 状态：路线待用户确认

## 变更目的

把 2026-08-18 需求—代码综合审查的正式报告转化为可追溯的整改输入。先重新核对当前代码、需求、ADR、Plan、Story、测试与发布配置，再按根因去重；不直接接受报告中的候选架构或 V2 Plan，也不修改业务代码。

## 文件增量

### 新增 `docs/test-reports/requirement-code-review-2026-08-18/remediation-ledger.md`

- 建立 50 项去重根因台账；每项记录正式来源、当前证据、影响 FR、分类、严重级别、目标工作线和处理状态。
- 建立 21 份正式报告覆盖矩阵，保留原始发现到根因 ID 的追溯关系。
- 将问题分为纯 Bug、需求/行为合同、跨模块架构与状态所有权、历史状态失真、报告描述偏差五类。
- 记录 ParaTranz JSON 双 ID 为已确认用户合同；`id != key` 不作为缺陷，缺陷是外部 ID 缺少有作用域的身份边界。
- 将 `rank-bm25` 结论校正为“已在 pyproject 声明，但未进入 lock/当前环境”，避免按过期描述重复修改。
- 提出九组候选跨模块工作线、依赖顺序、六项路线确认选择和 Phase 0 出口门禁。
- 明确三个 `.partial.md` 不是正式结论来源，且本阶段不修改 requirements、ADR、Plan、Story、业务代码或既有正式报告。

### 更新 `docs/changelogs/INDEX.md`

- 仅增加本次维护记录的索引入口；未重算或改写其他 Epic 状态。

## 当前事实复核

- 包/发布：console script 指向不存在的 `transbridge.main`，包版本双源不一致，包内仍大量使用 `src.transbridge`。
- 环境/依赖：项目 `.venv` 指向不可启动的 uv Python；7z/RAR 未声明；`rank-bm25` 已声明但未锁定且当前环境不可导入。
- Variant：运行时复现空 Variant 保留上一版本译文、清空译文后旧缓存值复活。
- Agent：注册 49 个工具后，translator/parser/editor/paratranz/writer 五个预置 Agent 工具数仍为 0。
- Tool Prompt：4180 字符完整帮助进入真实 observation 后仅剩 150 字符。
- Session/Task：`python -O` 下非法状态转换可执行；legacy callback 用原 callback 无法注销。
- ParaTranz Agent：当前 Project API 不存在工具调用的 `get_entries/upsert_entry/get_upload_history`。

## 验证

实际执行：

1. `git status --short --branch`、目标路径 `git status`/`git diff`：确认业务受控文件无现有修改；审查输入目录为未跟踪用户工件，本次仅归属新增台账。
2. PowerShell/`rg` 静态核验包入口、依赖、调用签名、状态所有权、checkpoint、FOMOD、Archive、Session/Task 与历史状态。
3. 系统 Python 3.13.5 只读运行时探针：复现 Variant、Agent wildcard、Tool Help、Session `-O`、Task listener 和 ParaTranz API 问题。
4. 定向测试：

   ```text
   python -m pytest -q \
     tests/test_fomod_pipeline.py tests/test_fileops.py \
     tests/smart_assistant/test_tool_prompt_layering.py \
     tests/smart_assistant/test_session_controller.py \
     tests/smart_assistant/test_session_controller_integration.py \
     tests/smart_assistant/test_mcp.py \
     tests/smart_assistant/postprocess/test_task_manager.py
   ```

   结果：122 passed，1 个 pytest cache 权限 warning。该结果与运行时阻断并存，证明现有测试没有覆盖真实组合根/成功链合同。
5. 台账复读：唯一根因 ID 数 50；正式报告覆盖行数 21。
6. 三个 `.partial.md` 仅计算 SHA-256 作为只读核对，未修改、未删除。

## 遗留项

- 用户尚未确认复杂路线与六项路线级选择；Phase 1 不得提前开始。
- 项目声明的 Python 3.12.12 `.venv` 当前不可启动；本阶段没有重建环境或改锁文件。
- 全量 QA、真实格式 corpus、clean wheel/install/CLI、Windows stdio、GUI 与外部服务验证均属于后续 Story/QA 门禁，本记录不宣称通过。
- 未执行 Git commit 或 push。
