# 综合整改复杂路线确认

- 日期：2026-08-18
- 归属：`maintenance/requirement-code-review-remediation`
- 阶段：bm-pilot Phase 0 → Phase 1 门禁
- 状态：已确认

## 确认内容

用户明确回复“好，开始”，确认 Phase 0 台账列出的六项路线级选择：

1. 采用模块化单体、Application Layer、Ports/Adapters 与单一 Composition Root，使用兼容 facade 渐进迁移。
2. MCP 主拓扑采用独立 `transbridge-mcp` stdio 进程；完整链路完成前必须安全降级。
3. Windows 正式分发采用安装器 + onedir payload，并另提供便携包。
4. 首批格式范围包含 ParaTranz JSON 双 ID、公开的 ESP/EET/XT 成功链和 Localized Strings 完整性；SST Writer 保持 experimental/关闭，其余历史格式进入支持矩阵确认。
5. 采用九组跨模块工作线，不按 FR1～FR16 分别补丁式整改。
6. 历史 Plan/Story 只做 `partially-verified`、`blocked_by`、`superseded_by` 等增量纠偏，不改写或伪造历史。

## 文件增量

- 更新 `docs/test-reports/requirement-code-review-2026-08-18/remediation-ledger.md`：状态改为“路线已确认；Phase 1 需求增量进行中”，勾选 Phase 0 出口门禁。
- 更新 `docs/changelogs/INDEX.md`：将维护线状态改为 Phase 1 进行中，并链接本记录。

## 验证

- 复读台账的六项路线确认内容，与用户确认的上下文一致。
- 检查现有记录编号，使用同日下一个编号 `002`。
- 未修改 requirements、ADR、Plan、Story 或业务代码；这些修改从 Phase 1 开始并遵循各自确认门禁。

## 遗留项

- Phase 1 需要把路线转写为可验证的行为、边界、失败语义和验收条件，并更新需求索引。
- 需求草案完成后必须暂停等待用户确认，确认前不得进入 ADR 或代码阶段。
- 未执行 Git commit 或 push。
