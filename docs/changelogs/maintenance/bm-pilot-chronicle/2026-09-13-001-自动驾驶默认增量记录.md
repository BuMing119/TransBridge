# 自动驾驶默认增量记录

- **日期**：2026-09-13
- **Epic**：`maintenance/bm-pilot-chronicle`
- **Story**：非计划 BM 工作流维护
- **Git 证据**：当前工作区中 `.agents/skills/bm-pilot/SKILL.md` 的任务归属差异；工作区其余已有变更不属于本次记录。

## 变更目的

让 `bm-pilot` 在任务产生实际变更时默认执行 `bm-chronicle`，避免端到端开发完成后遗漏可追溯增量记录。

## 文件级变化

- `.agents/skills/bm-pilot/SKILL.md`（修改）：在 skill 描述和直接、规划、架构三条路线中纳入增量记录阶段。
- `.agents/skills/bm-pilot/SKILL.md`（修改）：将 `bm-chronicle` 从用户、仓库规则或配置触发的可选阶段改为有实际变更时的默认收尾阶段。
- `.agents/skills/bm-pilot/SKILL.md`（修改）：保留 Chronicle 的归属安全边界；没有实际变更时不创建空日志，无法区分用户既有改动时暂停写入并询问归属。
- `.agents/skills/bm-pilot/SKILL.md`（修改）：明确最终报告包含增量记录，并要求复核记录后再完成任务计划。
- `docs/changelogs/INDEX.md`（修改）：新增本维护主题及当前记录的索引入口；不改动其他任务已经加入的索引条目。

## 验证证据

- `$env:PYTHONUTF8='1'; python -B C:\Users\admin\.codex\skills\.system\skill-creator\scripts\quick_validate.py .agents/skills/bm-pilot`：通过，输出 `Skill is valid!`。
- `git diff --check -- .agents/skills/bm-pilot/SKILL.md`：通过；Git 仅提示工作区行尾未来可能由 LF 转为 CRLF，没有空白错误。
- 首次未设置 UTF-8 的校验由系统 Python 按 GBK 读取中文，产生 `UnicodeDecodeError`；设置 `PYTHONUTF8=1` 后使用同一校验器通过。
- 本次仅修改工作流说明和增量索引，不涉及生产代码，因此未运行 pytest 或 Ruff。

## 遗留项

无。
