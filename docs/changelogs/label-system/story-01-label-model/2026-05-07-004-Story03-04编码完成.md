# 004: Story-03/04 编码 — 筛选更新 + AI 翻译适配

**日期**: 2026-05-07
**类型**: 改
**关联**: 自定义标签系统 > Story 03(筛选聚焦更新) + Story 04(AI翻译适配)

## Story-03: 筛选与聚焦更新（S01 中已覆盖）

`step2.py` 中 `_build_label_tags`、`_label_filters`、`_focus_labeled`、`_apply_all_filters` 标签筛选、`_update_count_label` 均已在 Story-01 中实现完毕，无需额外改动。

## Story-04: AI 翻译作用域适配

### `src/transbridge/ui/tools/ai_translator/ai_translator_window.py` (改)
- `_scope_mark_filters` → `_scope_label_filters`（全部 6 处）
- `_scope_mark_btns` → `_scope_label_btns`（全部 3 处）
- `_scope_mark_all_btn` → `_scope_label_all_btn`（全部 3 处）
- `_on_scope_mark_clicked` → `_on_scope_label_clicked`（全部 3 处）
- `_rebuild_scope_tags` 标记维度重写：从 `_step2._label_library` 读取标签库（替代 `_MARK_LABELS`）；从 `_step2._entry_labels` 统计使用次数（替代 `_entry_marks`）
- `_build_scope_candidates` 标记筛选改为标签交集匹配：`entry_labels.get(e.id, set()) & _scope_label_filters`
