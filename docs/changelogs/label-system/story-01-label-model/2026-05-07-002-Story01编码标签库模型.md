# 002: Story-01 编码 — 标签库模型与管理

**日期**: 2026-05-07
**类型**: 改/删/增
**关联**: Epic: 自定义标签系统 > Story 01: 标签库模型与管理

## 修改文件

### `src/transbridge/ui/workbench/step2.py` (改/删/增)
- **增**: `_LabelManagerDialog(QDialog)` 类——标签列表（QListWidget）+ 名称编辑（QLineEdit）+ 8 色预设颜色按钮 + 添加/删除；操作 `_labels` 副本，确认后写回
- **增**: `_ensure_default_labels()` ——首次使用时创建 3 个默认标签（待处理/有疑问/已确认）
- **增**: `_on_manage_labels()` ——弹出管理对话框，确认后更新 `_label_library`，清理已删除标签引用
- **增**: `_build_label_tags()` ——从 `_label_library` 动态构建标签筛选按钮（替代 `_build_mark_tags`）
- **增**: `_on_label_tag_clicked()` ——标签筛选点击
- **改**: 数据模型——`_entry_marks: dict[str,str]`→`_label_library: dict[str,dict]`+`_entry_labels: dict[str,set[str]]`；`_mark_filters`→`_label_filters`；`_focus_marked`→`_focus_labeled`
- **改**: `_init_ui`——标记筛选行增设「管理标签」按钮
- **改**: `get_selected_entries()`——返回有标签条目（替代返回 ★ 标记条目）
- **改**: `_populate_table`——Col 0 标记列改为彩色圆点（HTML span）+ tooltip
- **改**: `_apply_all_filters`——标记筛选→标签筛选（set 交集）；聚焦→只看有标签
- **改**: `_update_count_label`——"★ N / ? N / ✓ N"→"有标签 N 条"
- **改**: `refresh()`——标签数据清理 + `_ensure_default_labels` + `_build_label_tags`
- **删**: `_MARK_TYPES`、`_MARK_CYCLE`、`_MARK_COLORS`、`_MARK_LABELS`、`_ROW_BG_YELLOW` 常量
- **删**: `_build_mark_tags()`、`_on_mark_tag_clicked()` 方法
- **删**: `_on_cell_clicked` 中标记切换逻辑（改为 pass 占位）
