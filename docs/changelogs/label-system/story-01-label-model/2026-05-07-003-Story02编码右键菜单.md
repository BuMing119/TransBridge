# 003: Story-02 编码 — 右键菜单分配标签

**日期**: 2026-05-07
**类型**: 增
**关联**: 自定义标签系统 > Story 02

### `src/transbridge/ui/workbench/step2.py` (增)
- **增**: `_on_context_menu` — 右键位置计算行号 → 构建菜单 → 弹出
- **增**: `_build_context_menu` — 遍历 `_label_library`，每个标签 checkable QAction；分隔线 +「管理标签…」「+ 新建标签…」
- **增**: `_on_label_toggle` — 更新 `_entry_labels[entry_id]`（add/discard），重建标签筛选行 + 刷新表格
- **增**: `_on_quick_create_label` — QInputDialog 输入名称 → 随机颜色 → 加入 `_label_library` → 分配给当前条目
- **改**: `_init_ui` 中 table 设置 `CustomContextMenu` 策略 + 连接 `customContextMenuRequested` 信号
