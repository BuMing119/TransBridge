# 2026年1月5日
1. 写完了EET与XT的解析部分，暂未测试。
2. 写完了Json信息的数据结构，以及EET项的单向转换，未测试。

## 后续计划
1. 完成XT的Json项单向匹配。
2. 完成存储Json信息的存储与读取。
3. 完成XT与EET项的存储。
4. 完成Json项对XT项和EET项的导出与更新。

# 2026年1月8日
1. 完成了之前的全部计划
2. 完成了插件的更新和读取

## 后续计划
1. 完成Json项的读取相关
2. 完成逻辑相关的全部测试
3. 完成UI界面的设计

# 2026年1月9日
1. 完成了前面所有代码的测试
2. 完成了paratranz相关api接口代码的定义

## 后续计划
1. 完成UI界面的设计
2. 完成Json项目读取相关
3. 完成paratranz的互动
4. 自动分类，划分词条
5. esp存储的匹配（删除info的editid）

# 2026年3月5日

## Bug 修复
1. 修复了快速切换项目/标签时数据串位的问题，为全部 11 个异步组件引入 generation counter 模式，旧请求的回调不再更新 UI。
2. 修复了"我参与的"项目筛选不准的问题，改为直接向 API 传 `?uid=xxx` 参数，去掉了复杂的客户端成员缓存逻辑。
3. 修复了私信中联系人显示"用户1""用户10"等占位名的问题，改为解析 `fromUser`/`toUser` 嵌套对象中的真实昵称。
4. 修复了私信消息排序颠倒的问题，对话列表按最新在上排列，消息详情按最旧在上排列。
5. 修复了 `PluginWriter` 写回 ESP 时匹配失效（实际写入 0 条）的问题，旧代码构建的 key 格式与集合当前索引格式不符，已更正为 `editor_id:form_id|index~rec:sub` 完整格式，并删除了会误过滤 INFO/DIAL 对话条目的冗余校验。

## 词条详情优化
1. 将词条详情从容易误操作的内嵌右侧面板改为独立弹窗（`StringDetailDialog`）。
2. 导航列表实现自定义绘制，每行同时显示原文和键名（键名缩小淡化显示在下方）。
3. 修复选中项文字与背景颜色过于接近导致看不清的问题，改为使用系统调色板对调色，深色/浅色主题均保持高对比度。
4. 原文改为可上下滚动的文本框，设置最小/最大高度。
5. 导航列表支持翻页，与外层筛选参数联动。
6. 新增可折叠筛选面板，支持按翻译状态（多选）和最后修改者（互斥单选）过滤导航列表。
7. 为导航列表项及筛选面板各控件添加 tooltip，鼠标悬停可查看被截断的完整内容。

## 新增功能
1. 写回 ESP 插件：工作台步骤3"写回 ESP"按钮正式可用，选择输出路径后在后台将集合译文写入插件副本，完成后显示写入条数。
2. 词条创建：词条管理工具栏新增"创建词条"按钮（仅管理员可用），支持填写键名、原文、译文、所属文件、状态和上下文。
3. 历史记录词条 ID 筛选：历史记录标签页新增词条 ID 输入框，可按指定词条查询其全部变更历史。
4. 历史记录行详情：双击历史记录行弹出对话框，左右分栏完整展示修改前和修改后的内容，不受表格截断限制。
5. 贡献统计时间段筛选：贡献统计标签页新增开始/结束日期选择器，可按日期范围筛选贡献记录。
6. 讨论权限感知：讨论标签页切换项目时自动检查 `issueMode` 字段，内部模式下非成员、私密模式下非管理员均显示权限提示并禁用相关功能。

## 其他
1. 更新 `docs/functional_spec.md`，将已完成功能标注实际状态，补充实现位置和残余缺口说明。

## 后续计划
1. 完善批量操作：实现批量设置译文功能。
2. 术语历史查看：术语管理中"查看历史"功能尚未实现。
3. 错误处理统一化：401/403/429 等 HTTP 错误目前各组件各自弹窗，待统一处理。
4. Token 加密存储：当前明文存储在配置文件中。

# 2026年3月11日
1. Paratranz对接基础功能已经完成，为内测版v0.1

# 2026年3月18日
1. 优化bsa读取，修复写入bug，修正文件上传逻辑，，添加ai翻译，为测试版v0.11

# 2026年3月19日
1. 修复了eet的xml读取错误问题，为自动翻译空术语问题添加了提示，为测试版v0.111